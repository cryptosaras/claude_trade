# Analyst run 2026-08-02 ~06:00 UTC

## PRE-REGISTRATION (written and committed BEFORE any census or backtest was run)

Scheduled run. Yesterday's watchlist item 1 is this session, and it is the only
substantive item:

> **The 2026-07-06 event study's "+1.02% raw 8h hold" is untested out of its own
> window, and it is the evidence base for the only strategy still trading.**
> Yesterday did **not** refute it — `whale_absorb_runner` kept a 2.5-ATR stop, so
> it tested the wide-stop-no-clip variant, not the stopless raw hold. The clean
> test is a stopless 8h hold on post-study tape, and it has never been run.

Everything the live book rests on traces to that one document
(`strategies/whale_absorb.py` docstring): raw 8h-hold after an absorption event
**+1.02% net/trade, PF 3.09, n=175**, 30d ending 2026-07-06, 105 symbols, net of
0.16%. `whale_absorb` — the only strategy currently trading — exists because of
that number. If it does not reproduce on tape the study never saw, the live book
has no evidence base at all.

### Why this is a census, not a backtest

My first design was a `whale_absorb_hold` strategy file with a 50%-away stop, run
through the backtester against a `whale_absorb` control. **I am discarding that
design before running it, and recording why**, because the flaw is instructive:

- Every hold would run the full 8h, while the control exits at 1.5R in ~5h.
  With `max_positions_per_strategy: 3` and `max_positions_per_group: 2`, the hold
  variant would **decline entries the control accepted** — and the declines would
  not be random. They would fall on exactly the bursty absorption cascades this
  repo has documented (ORDI×2, TRB×5, JASMY×4). "Hold vs control" would partly be
  measuring *which trades each was allowed to take*, which is the slot confound
  already recorded in this repo's own notes.
- A 50% stop distance also drives `notional = risk/sl_dist` to ~123 USDT, a
  40× different sizing regime from the control.

**The census has neither problem.** It computes the forward 8h return from every
qualifying signal directly — no broker, no slots, no sizing, no leverage. That is
how the study itself measured, so it is a like-for-like replication rather than a
different experiment wearing the same name.

### What is measured

Rebuild the clause census (yesterday's throwaway tool; the "Note on method"
flagged it as worth rebuilding — this is the third session to need it). For every
closed 15m bar in the window, on every symbol, evaluate `whale_absorb`'s entry
conjunction **exactly as `should_enter` does** — `vol ≥ 2.5× avg20`,
`range ≤ 0.9 ATR`, `low within 0.3 ATR of the 12h low`, `6h return ≤ −1.5%`, and
the resulting 2.5-ATR stop distance inside [0.5%, 5%]. Then, from each signal,
measure three variants **on the identical signal set**:

| variant | exit rule |
|---|---|
| **RAW** | hold 32 bars (8h), no stop, no target — *the study's claim* |
| **STOP** | 2.5-ATR stop below the absorption low, else 8h |
| **STOP+TP** | 2.5-ATR stop, 1.5R target, else 8h — *the deployed `whale_absorb`* |

All three net of **0.16%** (0.10% fees round-trip at `fee_taker` 0.0005/side plus
0.06% slippage round-trip at 0.0003/side), which is the same 0.16% the study
states. **No fee or slippage number is touched** — these are read from
`config/settings.yaml`, not chosen.

Entry price is the **close of the signal bar**; the live engine enters at the next
tick, a uniform +15 min later. Stated, not hidden: the census is 15 minutes early
relative to live, on a 32-bar horizon.

### Window, pinned now

**Signal bars with close in [2026-07-06T00:00Z, 2026-08-01T21:00Z].** This is the
entire post-study tape and it is pinned before looking. The 8h horizon puts the
last exit at 2026-08-02T05:00Z, inside available data.

**I am deliberately NOT splitting this into two 14-day halves.** There is no
tuning happening — the horizon (32 bars) and every entry param were declared by a
document written on 2026-07-06 — so both halves would be confirmation runs, and
splitting would halve the power of a test whose entire point is n. One window,
reported with its n.

**Universe:** the study used 105 symbols; the trading groups today hold ~72 and
the collector has 202. **I census all collected symbols with sufficient history**
(the honest replication) **and report the ~72 group-symbol subset separately**
(the deployable subset). Pre-declaring both so neither can be selected after the
fact.

### Pre-committed predictions, sharpened to be falsifiable

1. **The post-study raw 8h hold comes in below +0.50% net/trade** — i.e. less than
   half the study's +1.02%. One number, falsifiable in one direction. If it clears
   +0.50% the study survives post-study tape and the next session gets a
   pre-declared mid-width-stop candidate; if it does not, the evidence base for
   the only live strategy is broken, and that is the finding.
2. **Ordering: RAW > STOP+TP** — the stop costs money, as the study claims
   ("a tight SL destroys it", PF 0.58). If STOP+TP beats RAW, the study's central
   mechanism is inverted and I will say so.

I am recording no third prediction. Yesterday two of my pre-registered predictions
were wrong (binding clause; loss size), both because I padded the list.

### Validation the census must pass, or it is void

The census is a measurement of the live engine only if it reproduces the live
engine. `whale_absorb` has **9 live entries**. Pre-declared bar: **the census must
contain ≥5 of the 9 live entries at the exact symbol with the signal bar 15 min
before the live entry timestamp.** Below that, the census is void and this session
reports a tooling failure rather than a result. (Yesterday's build hit 5 of 6 on
the episode it checked.)

Census runs **twice** and requires byte-identical output, same as any backtest.

### Pre-committed regardless of outcome

- **No promotion, no status change, no param edit, on any strategy, whatever the
  numbers say.** This session measures a premise; it does not ship a strategy.
  A stopless variant is not deployable as-is, so a positive result buys the *next*
  session a pre-declared mid-width-stop candidate — not this one a promotion.
- **No `meta.regimes` edit anywhere.**
- **`whale_absorb` gets no verdict** — it is n=9 live, sixth consecutive session
  below the gate.
- **No change to the `n ≥ 20` gate** (human call, Jul 31).
- **No unpausing of `vwap_revert_short`** (Jul 21 human call).
- **No `open_interest` work** (mid-August, unchanged).
- **No re-opening of `pump_fade`** (closed Jul 29) or `whale_absorb_runner`
  (rejected Aug 1).

### Standing checks carried in

- Any run with a non-empty `.err` is void, not a result.
- Twinned runs, identical output required before a number is used.
- Filter `tf='1m'` on any candle query.
- Per-symbol concentration reported on any edge claimed.

### Expected outcome

Most likely: prediction 1 lands (raw hold well below +1.02%), prediction 2 lands
(the stop costs money), and the session's finding is that **the only quantitative
claim under the live book does not survive its own out-of-sample window** —
recorded, with no action taken, because acting on it is a strategy decision for a
session that has a replacement candidate.

---

# OUTCOME (read this first)

- **The 2026-07-06 event study does not replicate on the tape it never saw.**
  Its headline raw 8h hold was **+1.02% net/trade (PF 3.09)**. Reconstructed on
  Jul 6 → Aug 1 across 132 symbols, the same event's raw 8h hold is
  **−0.356% net/trade (PF 0.732, n=285)** — a **1.38 percentage-point** swing,
  and the sign is inverted.
- **The deployed configuration fails the same way, and this is the number that
  matters** because it is what `whale_absorb` actually trades. The study reported
  wide-SL + 1.5R-TP, alt groups, BULL/BEAR at **+0.31%/trade, PF 1.36, n=108**.
  The census on the identical config, post-study: **−0.081%/trade, PF 0.894,
  n=56.**
- **My second pre-registered prediction was wrong, and wrong in the direction
  that inverts the study's mechanism.** I predicted RAW > STOP+TP ("the stop
  costs money", per the study's PF 0.58). Measured: **the stop is protective in
  every universe**, by 0.30–0.38 pp. RAW < STOP < STOP+TP, three for three. That
  is not a magnitude miss; it is a sign flip on the study's central claim.
- **Prediction 1 was right, by a wide margin.** I pre-declared "below +0.50%";
  it came in at −0.356%.
- **The census reproduced the live engine: 7 of the 9 live `whale_absorb`
  entries**, each at a uniform **+15.0 to +15.7 minute** offset (bar close →
  next tick). Pre-declared bar was ≥5 of 9. **Both twins byte-identical**
  (`md5 d21e8ade…`), both `.err` empty.
- **The two misses fail on exactly one clause each — `range ≤ 0.9 ATR` — and
  that is a live-engine finding, not census hygiene.** See Finding 4: a
  collector gap inside a 15m bar makes the bar look artificially narrow, and
  "narrow range" is the precise thing this strategy keys on.
- **The study is unauditable.** Its numbers exist only in a docstring and the
  commit message of `99cd05b` (written by a concurrent Fable session on
  2026-07-06). **There is no report, no script, no artifact for it anywhere in
  this repo.** The only strategy still trading rests on an unreproducible number
  that does not replicate when reconstructed.
- **As pre-committed: nothing was promoted, retired, edited, re-tuned or
  re-gated.** No `meta.regimes`, no param, no status, in file or DB.
  `whale_absorb` gets no verdict — n=9 live, sixth consecutive session.

## Health — no action

- Collector and engine alive; server time 06:02Z. Equity **6,160.23 (−38.4%)**,
  **0 open positions**.
- **One trade since the last session** — `whale_absorb` long `APE_USDT`, entered
  2026-08-01 20:45Z in BEAR, closed 04:45Z today at the 8h exit for **+34.94**.
  That takes `whale_absorb` to **n=9 live: net −272.61, 3 wins (33.3%)**, exit
  mix **6 stop-loss / 2 bounce-window-over / 1 take-profit**. Both time-exits
  were winners; all six stop-outs were losses.
- Regime **BEAR** since 2026-08-01 19:04Z (ADX 22.8, BTC 63,430).
- **The MEXC 510 quiet streak ended**: one `510 Requests are too frequent` at
  2026-08-01 19:31Z, after 8 consecutive clean sessions. A single occurrence, no
  gap in trading followed it. Monitor only.
- No error or strategy-crash events otherwise.
- Book unchanged at 17 strategies, **16 of them not trading**.

## Finding 1 — the replication, three exit variants on one signal set

Window **Jul 6 00:00Z → Aug 1 21:00Z** (26.875 days), pinned before looking, and
fully disjoint from the study's ~Jun 6 → Jul 6. **338,015 closed 15m bars** on
**132 symbols** with ≥90% 1m coverage (of 202 collected). Every variant below is
measured on the **identical 285 signals** — no broker, no slots, no sizing — and
is net of **0.16%** (0.10% fees + 0.06% slippage, read from `settings.yaml`).

**All collected symbols (n=285):**

| variant | mean %/trade | win% | PF | avg win | avg loss |
|---|---|---|---|---|---|
| **RAW** (8h hold, no stop/target) | **−0.356** | 45.96 | **0.732** | +2.122 | −2.465 |
| **STOP** (2.5 ATR, else 8h) | −0.044 | 38.95 | 0.954 | +2.336 | −1.563 |
| **STOP+TP** (2.5 ATR + 1.5R = *deployed*) | −0.061 | 42.46 | 0.933 | +2.008 | −1.587 |

**Group symbols only (n=172):** RAW −0.598 (PF 0.523) / STOP −0.368 (0.617) /
STOP+TP −0.276 (0.698).

**The deployed subset — group symbols, alt groups, BULL+BEAR (n=56):**
RAW −0.464 (PF 0.571) / STOP −0.243 (0.706) / **STOP+TP −0.081 (PF 0.894)**.

Head to head against what the study claimed:

| | study (Jun 6 → Jul 6) | census (Jul 6 → Aug 1) |
|---|---|---|
| RAW 8h hold | **+1.02%**, PF 3.09, n=175 | **−0.356%**, PF 0.732, n=285 |
| deployed config | **+0.31%**, PF 1.36, n=108 | **−0.081%**, PF 0.894, n=56 |

**How much of this is noise?** Measured from the 285 per-signal returns (not
assumed): **σ = 4.06%**, so **SE = 0.240%** at n=285. Therefore:

- **+1.02% is excluded at 5.7 SE.** That is not a sample-size argument, and it is
  the claim this session was built to test.
- **−0.356% is only 1.5 SE below zero — indistinguishable from zero.** This repo's
  own power note puts the "covers costs vs no edge" question at 266–897 trades,
  and n=285 sits at the bottom of that band.

So the honest claim is **"the claimed edge is absent", not "the edge is
negative"** — and the second half of that sentence is load-bearing: this census
does **not** establish that the signal loses money, only that it does not make
the +1.02% the strategy was built on.

**Concentration**, per the standing rule: 285 signals over **98 distinct
symbols**; the top 10 hold **25.6%** and the largest single symbol (`RE_USDT`,
13) is **4.6%**. The negative result is diffuse, not one coin.

**A limitation I am stating rather than glossing.** The census fires **0.087
conjunctions per symbol-day** against the study's implied **0.056** — about
**1.5× more often**. My reconstruction is therefore *not* byte-equal to the
original event definition (which no longer exists to compare against). What
licenses the census as a measurement of **the live engine** is the 7-of-9 entry
reproduction below, not agreement with the vanished study code.

## Finding 2 — the stop is protective, which inverts the study's mechanism

The study's stated mechanism: *"a tight SL below the absorption low destroys it
(PF 0.58 — chop consumes it before the bounce); only a WIDE stop survives."* The
implication I pre-registered from it was that the stop is a cost paid to make the
signal tradeable.

On post-study tape it is the opposite — adding the 2.5-ATR stop **improves**
mean return in every universe:

| universe | RAW | → STOP+TP | improvement |
|---|---|---|---|
| all collected (285) | −0.356% | −0.061% | **+0.295 pp** |
| group symbols (172) | −0.598% | −0.276% | **+0.322 pp** |
| deployed subset (56) | −0.464% | −0.081% | **+0.384 pp** |

Three universes, same direction. The mechanism reads plainly in the win/loss
split: the stop cuts the average loss from **−2.465% to −1.587%** while the
average win only falls from **+2.122% to +2.008%**. **There is no bounce to
protect** — after these events price keeps going down, and truncating that is
worth more than the winners it clips.

This also settles yesterday's open question in the opposite direction to my
reasoning. Yesterday `whale_absorb_runner` (stop kept, TP clip removed) was worse
than the control, and I concluded the bounce "reverses" rather than running to
6R. The census says something stronger and simpler: **the 8h forward drift after
these events is negative, so every mechanism that keeps the trade alive longer
loses money.** `whale_absorb`'s remaining PF comes from cutting trades short, not
from an edge in the event.

## Finding 3 — the study is unauditable, and that is the structural finding

I looked for the artifact behind `+1.02% net, PF 3.09, n=175`. It does not exist:

- `git log -- strategies/whale_absorb.py` has two commits. The numbers first
  appear in **`99cd05b` (2026-07-06)**, in the docstring and the commit message.
- That commit is `Co-Authored-By: Claude Fable 5` — a **concurrent session**
  working the same clone that day. This session's own report for 2026-07-06
  (`reports/2026-07-06-1409.md:18`) records only *"2nd session created
  whale_absorb v1 (paused, pre-gate)"*.
- **No study script, no output file, no report section anywhere in `reports/`
  or the tree** contains the numbers. `grep` for `3.09`, `n=175`, `+1.02%`
  returns only the docstring, `whale_absorb_runner`'s docstring, and reports that
  cite the docstring.

So the evidence chain under the only live strategy is: *a number in a comment,
cited by later reports as though it were a measurement.* Two sessions (Aug 1 and
today) have now built work on it. **This is the finding with the longest shelf
life in this report**, and it generalises: a claim with no reproducible artifact
should not be cited as evidence, however precisely it is quoted.

I am **not** claiming the original study was wrong when it was run. Jun 6 → Jul 6
may genuinely have looked like that. What is established is narrower and enough:
**reconstructed on the next 27 days, the claim does not hold, and the original
cannot be checked.**

## Finding 4 — a collector gap can manufacture this strategy's signal

The census matched 7 of 9 live entries. The 2 misses were probed directly, with a
known-good entry as control:

| live entry | vol | **range/ATR (limit 0.9)** | low-prox | 6h drop | SL dist |
|---|---|---|---|---|---|
| `ORDI` Jul 28 17:30 *(control, matched)* | 2.85× ✓ | **0.777 ✓** | ✓ | −2.23% ✓ | 1.63% ✓ |
| `UNI` Jul 27 21:30 *(missed)* | 3.61× ✓ | **1.423 ✗** | ✓ | −1.90% ✓ | 1.43% ✓ |
| `JTO` Jul 29 03:30 *(missed)* | 3.37× ✓ | **0.911 ✗** | ✓ | −1.91% ✓ | 1.42% ✓ |

**Both misses fail on exactly one clause, and it is the same clause: the bar is
wider than 0.9 ATR in today's data than it was when the engine traded it.** JTO
misses by 0.011 ATR (1.2% of the threshold) — within ATR-warmup sensitivity and
unremarkable. **UNI at 1.423 is not**: the engine would have needed an ATR 58%
larger, which warmup cannot produce.

**Leading hypothesis, explicitly unverified:** the engine saw an *incomplete* 15m
bar. A missing 1m candle inside 21:15–21:30 makes the resampled bar's high/low
narrower, the collector backfills the minute later, and today the bar is wider.
The direction fits exactly — and the consequence is uncomfortable, because
**`whale_absorb`'s core clause is "huge volume, no price progress". A data gap
removes price progress without removing volume. It manufactures the signal.**

**This is not verifiable from the DB**: `candles` has primary key
`(symbol, tf, ts)` and no insert timestamp, so there is no record of what was
present when. Stated as a candidate, put on the watchlist, not acted on. Any fix
(a bar-completeness guard in the strategy or the engine) is a code change that
needs its own pre-registration.

## Supplementary — POST-HOC, exploratory, no action taken

Everything above is the pre-registered result. **Everything in this section was
computed after seeing it**, from a third run that dumped the 285 per-signal
records. That run's aggregate block is **byte-identical to both twins**, so the
dump is the same measurement, not a new one — but the *cuts below were chosen
after the fact* and are labelled accordingly. Standard errors are shown on every
cell precisely so none of them can be read as a finding.

| regime | variant | n | mean %/trade | SE | PF |
|---|---|---|---|---|---|
| BULL | RAW | 76 | **−1.128** | 0.550 | 0.369 |
| BULL | STOP+TP | 76 | +0.039 | 0.255 | 1.045 |
| BEAR | RAW | 16 | **+1.019** | 0.721 | 3.196 |
| BEAR | STOP+TP | 16 | +0.024 | 0.615 | 1.027 |
| SIDE | RAW | 193 | −0.166 | 0.271 | 0.864 |
| SIDE | STOP+TP | 193 | −0.108 | 0.155 | 0.884 |

**The BEAR cell needs naming before someone else finds it.** BEAR RAW is
**+1.019%, PF 3.196** against the study's **+1.02%, PF 3.09**. That is an almost
exact numerical match and it means **nothing**: n=16 is below this repo's own
`n ≥ 20` floor, its SE is 0.721 so it sits **1.4 SE from zero**, and it is one of
three post-hoc buckets. Recording it here so the next session reads it as the
coincidence it is rather than rediscovering it as evidence.

**The most negative cell is BULL RAW (−1.128%, 2.1 SE below zero)** — and it is
*not* a case for narrowing `whale_absorb`'s regimes, because the deployed
variant in that same bucket (BULL STOP+TP) is **+0.039% ± 0.255, i.e. flat**. A
post-hoc regime split on a variant the strategy does not trade is not evidence
about the variant it does.

**By group, STOP+TP, all regimes:** `mid_alts` −0.351 ± 0.205 (n=102),
`large_alts` −0.263 ± 0.212 (n=46), `memes` +0.017 ± 0.334 (n=24), and
non-group collected symbols +0.266 ± 0.242 (n=113).

**The summary of the whole supplementary is that there is nothing in it.** No
cell is more than ~2 SE from zero. The deployed configuration is flat-to-slightly
negative in every regime and every group. There is no surviving pocket to retreat
into, which is itself worth knowing — it forecloses the obvious next move of
narrowing `whale_absorb` to whichever bucket looked best.

## Actions taken

1. **None to the trading system.** No strategy created, edited, promoted,
   retired, paused or re-gated. No `meta.status`, `meta.regimes`, or param
   touched, in file or DB. No config or universe change. `whale_absorb` remains
   `incubating` at n=9 with no verdict — sixth consecutive session.
2. **Measured, twinned and recorded** the pre-registered census (this report).
3. **Corrected one of my own numbers within this report**: Finding 1 first
   estimated the standard error from an assumed σ ≈ 3%; the measured σ is 4.06%,
   so the exclusion of +1.02% is **5.7 SE**, not "7–10", and −0.356% is **1.5 SE**
   from zero, not 2. The correction weakens my own headline in the honest
   direction and is left visible rather than smoothed over.

## Rejected / not done

- **Retiring `whale_absorb`.** The temptation is obvious and I am declining it
  on the pre-registration, and on merit: live n=9, and the census's own verdict
  is "no measurable edge", **not** "loses money" (1.5 SE from zero). Retirement
  is a decision for a session that pre-registers it as its question.
- **The `whale_absorb_hold` strategy file with a 50% stop.** Discarded at design
  time for the slot confound, before it was run — recorded in the
  pre-registration so this reads as a design decision, not a buried result.
- **Narrowing `whale_absorb` to BEAR** (where RAW looked like the study) or
  **dropping BULL** (where RAW looked worst). Both are post-hoc splits at
  n=16/n=76, and the deployed variant is flat in both buckets.
- **Re-tuning the stop now that it is known to be protective.** That inverts a
  mechanism on one out-of-sample window; turning it straight into a parameter
  search is the fit-to-the-result error the pre-registration exists to prevent.
- **Fixing the bar-completeness hazard in Finding 4.** It is an unverified
  hypothesis about a live engine that is currently trading. It needs its own
  pre-registration.
- **Changing the `n ≥ 20` gate** (human call, Jul 31), **unpausing
  `vwap_revert_short`** (human call, Jul 21), **`open_interest` work**
  (mid-August), **re-opening `pump_fade`** (closed Jul 29) or
  `whale_absorb_runner` (rejected Aug 1). All unchanged.

## Watchlist / next session (ranked)

1. **`whale_absorb` should now be pre-registered for a retirement decision.**
   The evidence is: live n=9 net −272.61; deployed config −0.081% (PF 0.894) on
   26.9 days of post-study tape; its founding study unreproducible; and no
   regime or group bucket that survives. What is *missing* is a pre-registered
   decision rule written before looking again. **Write that rule first, then
   look** — and note the counter-argument honestly: nothing measured is more
   than ~2 SE from zero, so this is "no evidence of edge", not "evidence of
   harm". If it is retired, the book trades nothing at all, which is a state the
   human may want to know about in advance.
2. **The bar-completeness hazard (Finding 4).** A collector gap inside a 15m bar
   narrows the bar and can manufacture a "no progress" signal. Unverifiable
   retrospectively (`candles` has no insert timestamp). **Two things are cheap:**
   add an insert/updated timestamp column so this is answerable in future, and
   census how often 15m bars in the live path are assembled from <15 1m candles.
   Both are measurements, not fixes.
3. **The methodological rule this session establishes: a number with no
   reproducible artifact is not evidence.** The `+1.02%` propagated through at
   least three reports and one candidate strategy on the strength of a docstring.
   Worth applying to the rest of the book — several retired strategies carry
   study numbers in their docstrings with no artifact either.
4. **The census tool is now worth keeping.** Third session to need it, and this
   time it produced a reusable per-signal dump. It answers "did the tape offer
   this setup, and what happened next" far cheaper than a backtest, with no slot
   or sizing confound. Promoting it out of `delete/` into a real tool is a small
   piece of work with repeated payoff.
5. **The design constraint from Aug 1 stands and is now harder.** ≥0.30% gross
   per trade needs 38.4% win rate at the book's R:R 2.64, or 23.3% at R:R 5. The
   exit lever (Aug 1) and the stop lever (Jul 31) are both closed, and today the
   *signal* under the last live strategy failed too. **The next candidate must
   come from a new signal, and its edge must be visible at n~300, which means
   frequency is a design requirement.**
6. **MEXC 510s: the quiet streak ended** with one event on Aug 1 19:31Z after 8
   clean sessions. Monitor.
7. **Cosmetic drift, deferred again:** `BONK_USDT` (0 candles), and the
   `btc_shock_fade` / `vwap_revert_short_bull` file-vs-DB status mismatch.

## Note on method

Two things worked this session and are worth repeating. **Pre-registering the
*design* and not just the numbers** caught the slot confound in the backtest
approach before it produced a result I would then have had to caveat — the
discarded design is in the record above. And **twinning the census the same way
backtests are twinned** (byte-identical, `.err` empty, plus a third run whose
aggregates matched) meant no part of this rests on a single execution.

One thing to carry forward: the pre-declared **void condition** (≥5 of 9 live
entries reproduced) was worth more than the predictions. It is what separates
"my tool measures the live engine" from "my tool measures something I wrote",
and it is the check that turned the 2 misses into Finding 4 instead of a
footnote.
