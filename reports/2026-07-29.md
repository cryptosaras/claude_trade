# Analyst run 2026-07-29 ~17:00 UTC

## PRE-REGISTRATION (written and committed BEFORE any backtest was run)

This session executes **watchlist item 1** from Jul 28: decide `pump_fade`
properly, with the gate written before the runs rather than after the numbers.

### Fingerprint FIRST — and window B has already changed

Measured at 16:52Z, before any run:

| window | span | pinned command | 1m rows | distinct symbols |
|---|---|---|---|---|
| **A** | Jun 9 – Jun 25 | `--days 16 --end 2026-06-25T00:00` | 3,505,982 | 180 |
| **B** | Jul 13 – Jul 27 | `--days 14 --end 2026-07-27T00:00` | **3,076,469** | **173** |

Jul 28 recorded window B at **171 symbols / 3,037,271 rows**. It is now
**173 / 3,076,469** — **+2 symbols and +39,198 rows inside a supposedly pinned
window.** This is exactly the caveat my memory note carries: the collector
backfills *past-dated* candles for newly discovered symbols, so a pinned window
is reproducible only while the universe gains no new symbols.

**Pre-committed consequences, before seeing any result:**

- The Jul 28 figures (`pump_fade` n=45 / PF 1.20; `pdhl_sweep` n=59 / PF 1.07)
  are **historical record only**. They are not expected to reproduce and a
  deviation from them is **data change, not nondeterminism**, and will not be
  investigated as a methodology failure.
- **Today's twinned numbers are the decision inputs.** Nothing decided today
  cites a Jul 28 number.
- If the **two runs within a twin pair** disagree, *that* is a methodology
  problem, it takes priority over the decision, and no status changes.

### The gate — the Jul 27 two-window rule, unchanged and unweakened

> Promote `pump_fade` from `retired` to `incubating` **iff n ≥ 20 AND net
> PF ≥ 1.10 on BOTH windows**, each run twice with identical output.

Branches, all fixed now:

1. **Both windows pass** → promote to `incubating` in file *and* DB. **Not
   `active`** — validation-gate step 3 still requires ≥5 days and ≥20 live
   trades before any promotion beyond incubation.
2. **Window A fails** (PF < 1.10 or n < 20) → **stays `retired`, and the
   question is CLOSED.** Recorded so that no future session re-opens it on
   window B alone.
3. **Twins disagree within a pair** → methodology problem; no status change.
4. **Any run errors** (non-empty `.err`) → the run is void, not a result.

### The pooled arithmetic, stated in advance so it cannot be argued away later

Jul 28's values: window A **net −841 (n=144)**, window B **net +549 (n=45)** →
**pooled −292 over n=189**.

**Rule:** if the twins broadly reproduce that shape, the pooled record is
negative and `pump_fade` stays `retired` **regardless of how the split reads.**
This is written now specifically to foreclose the "but window B is the clean
window" argument arriving after the fact.

### Explicitly OUT of scope, pre-committed

- **No regime narrowing.** Its docstring already records that the dev edge was
  7 SIDE trades at PF 1.89, and window B reads SIDE 38 @1.50. Narrowing
  `meta.regimes` to SIDE *after* seeing that is tuning on the evidence. It may
  be worth testing — with its own pre-registration, threshold written before
  the split is computed. Not here.
- **No window C.** A third window (Jun 25 – Jul 13) was considered and dropped.
  A number reported with no rule attached to it is how a "no" becomes a
  "well, but".
- **No `whale_absorb` judgement.** It is at n=8 live. The gate wants ≥20.

### One diagnostic added — and why it is not a loophole

The runs also dump **distinct-symbol count and trades/day**. Reason:
`pump_fade`'s own docstring records its dev study as **n=19 over 21 days**,
while window A produced **n=144 over 16 days** — a ~8× trade rate on
overlapping tape. Its entry needs a ≥4%/1h run, so a high-pump window fires far
more often. If A and B differ hugely in firing rate they may not be two draws
from one population, and "the bigger sample says no" would be a category error
rather than an argument.

**This diagnostic cannot promote anything.** It has no threshold and no branch
attached. It only tells the reader whether the pooled-arithmetic rule above is
sound reasoning or a mis-comparison — and the rule applies either way.

### Secondary, only if the primary finishes with time to spare

Watchlist item 3: `pdhl_sweep` **`max_sl_pct` DOWNWARD** (currently 0.04, which
never binds). Finding 4d measured its per-bucket × hurdle as +1.59 / **+2.08
peak** / +1.20 / **−0.54** / **−4.17** across rising stop distance, so the
losing tail is the *upper* one.

- Derived candidate: **`max_sl_pct` 0.04 → 0.010**, which cuts both losing
  buckets. Tune on **window A** (in-sample, hence useless as a test but correct
  as the gate's step-1 tuning window), confirm **once** on window B.
- **Pre-committed:** a window-B improvement driven by a band holding **n < 20**
  is **NOT** a pass. This is the failure mode of Finding 4 with the sign
  flipped, and the two buckets motivating the test are n=10 and n=7.
- Report the variant's **n and realized stop distribution alongside PF** — the
  slot-reshuffle confound applies exactly as it did last session.
- **No status change from this test regardless of outcome.** `pdhl_sweep` stays
  `retired`; a pass makes it a candidate for a human-attended re-tune.

### Expected outcome

**Most likely: zero status changes and a closed question.** The pooled record is
already negative and window A is largely in-sample for this strategy. A "no"
with the arithmetic on record is a complete deliverable.

---

*(Results follow below, appended after the runs.)*
