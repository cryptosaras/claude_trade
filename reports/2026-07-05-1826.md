# Analyst run 2026-07-05 18:26 UTC (run 3)

## Task (human-directed)
Expand the trading universe: add 50 coins fitting our strategies, after first
verifying the VPS/engine can handle them. Done: trading symbols 24 -> 74.

## Capacity check (the "will it handle it" question — measured, not guessed)
Measured on the 2-vCPU box with an in-memory broker (no side effects):
- Full engine tick = candle load (3500x1m + 450x1h per symbol) + `core.step`:
  - N=24: 1.37s (load 1.07 / step 0.30)
  - N=69: 2.94s (load 2.44 / step 0.50)
  - extrapolated N=74: **~3.1s (16% of the 20s tick)**; N=100: ~4.0s (20%).
  `core.step` is only ~0.5s — candle load dominates, and both are linear in
  symbol count. **Compute is NOT the binding limit up to ~100 symbols.**
- RAM: engine 66MB, ~2.1GB free on the box. Disk 35% used; +30 new collected
  symbols ~= +0.3GB/month. Collector steady state ~2 MEXC req/symbol/cycle;
  ~200 req/cycle at 99 collected @8rps => ~40s candle freshness (fine for the
  1-12h mandate). The old universe.yaml "<=60" note was conservative; updated
  it with these numbers. **Verdict: VPS handles +50 with ~17s of tick headroom.**
- Confirmed in production post-deploy: engine heartbeat advanced 19.8s over 22s
  wall at 74 symbols — tick cadence intact.

## Why add symbols at all (select "by strategies")
Active strategies are **signal-starved**, not slot-saturated: 12 live trades in
the system's ~7-day life, 0 open now (breakout_momentum, now retired, was 8 of
them). The goal needs >=30 trades/14d, so widening the symbol surface is the
right lever. Weighted the adds to where the 4 active strategies actually trade.

## Selection method
Pulled all 959 MEXC contracts (`/api/v1/contract/ticker` + `/detail`). Filtered
out tokenized-stock/commodity/index synthetics using the `conceptPlate`
`mc-trade-zone-tradfi` tag (clean discriminator: SPX500/NVIDIA/XAU/*STOCK carry
it, real crypto never does — those track TradFi and ignore our BTC-anchored
regime). From 564 tradeable crypto candidates, picked recognizable/liquid names
matched to each group's behavior. Dropped high-turnover unidentifiables (e.g.
LAB $92M — likely wash volume) and the 1000BONK dup.

Distribution (24 -> 74):
- **large_alts +22** (4 active strategies: range_fader / funding_skew_fade /
  ema_trend_rider / vwap_revert_short) — ZEC TAO WLD XLM ENA ONDO AAVE HBAR BCH
  LTC XMR FET ATOM UNI ICP DOT LDO JUP RENDER POL ETC PYTH.
- **mid_alts +20** (vwap_revert_short + sweep_reclaim) — TLM SYN ETHFI EIGEN RPL
  ORDI KAS GALA JASMY DYDX TRB JTO VIRTUAL XTZ DASH APE CRV GRASS W ALGO.
- **memes +8** (only incubating sweep_reclaim consumes memes; pump_fade retired,
  so this is a small bet) — FARTCOIN PENGU PIPPIN SPX TURBO ACT GOAT PNUT.
  Sub-$1M ones (TURBO/ACT/GOAT/PNUT) are thin — on a short leash.

20 of the 50 were already collected (zero backfill); 30 were new.

## Verification
- Pushed 260d977 -> autopulled in ~80s (config hot-read, no rebuild needed).
- Collector re-read universe, backfilled all **50/50 new symbols to 30d 1m**
  (~4-5 min for the 30 new), cycle completed at trading_groups=74, 101 symbols.
- Engine already trading the 74-symbol universe, healthy tick, no error events.

## Caveats / gate
- Can't pre-backtest uncollected coins. Honest gate here = live-paper + **prune
  per group after >=20 trades** (`/api/strategies` stats_by_group). Next runs:
  watch which new members actually generate signals; drop dead weight, keep the
  producers. Adding symbols does not add position slots (8 total, 2/group) — it
  only broadens the setup pool.
- 74 > the old soft cap of 60, justified by the measurement above.

## Context noticed (another actor on main)
- Commits b606849 (backtester charges real historical funding) and 4b977f7
  (cross-symbol ctx: ctx.peers/ctx.btc + dispersion_fade v1, paused) landed
  mid-session via autopull rebuild. dispersion_fade is paused pending gate; if
  unpaused it may consume the new large_alts/mid_alts members too.

## Backlog (carried + new)
- [testing] sweep_reclaim incubation — now has a much larger mid_alts+memes
  surface to gather its >=20 trades on.
- [next run] Pull stats_by_group once the new symbols have live trades; prune
  members with no edge. Especially audit the 8 memes.
- [carried] Funding-history replay in backtester (~Jul 18); OI-surge strategies
  (~Jul 19); regime-age filter for sweep_reclaim v4.
- [note] BTC lead-lag idea from run 2 is now partly shipped (ctx.btc exists via
  4b977f7).
