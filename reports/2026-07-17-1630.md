# Analyst run 2026-07-17 16:30 UTC

Headline: **retired `pdhl_sweep`** on its own pre-registered forward kill —
the first clean, actionable decision in three sessions, because it rests on
**live forward stats, not the backtester**. vwap_revert_short holds (healthy,
edge still growing). BEAR lane is one session away from its drop trigger.

## Market read
- Regime **BEAR** (conf 0.83, ADX 28.9, ema_slope_24h −0.0017), BTC ~63.3k.
  But the regime is **flickering**: SIDE↔BEAR flipped ~6× in the hour to 15:03Z
  (14:04 BEAR, 14:10 SIDE, 14:34 BEAR, 14:40 SIDE, 14:51 BEAR, 14:57 SIDE,
  15:03 BEAR). This is boundary chop, not a decisive trend — treat any
  regime-tagged split from this window with suspicion (see BEAR lane below).
- Equity **7198** (−28.0% lifetime), up ~250 vs Jul 16 (6952). Today −44 on 19
  closes — churny (many −85→−94 stops interleaved with a few +190→+325 wins),
  the normal short-fade texture.
- **2 open shorts**, both vwap_revert_short mid_alts, both BEAR-entry:
  INJ_USDT @ 5.089 (6.4 ATR above VWAP, RSI 71) and VIRTUAL_USDT @ 0.641
  (5.7 ATR, RSI 69). Both textbook high-stretch entries.

## Health
- collector + engine alive, no crashes, no error-level events. Only benign
  `funding history backfilled for 1 symbols` messages (13:00, 13:42, 14:29,
  15:19Z) — same append-of-past-dated-rows pattern noted Jul 16; it's the
  reason pinned windows can shift across days, harmless intraday.

## ACTION: retired pdhl_sweep (7d3c660)
The strategy set its **own** kill line in its docstring (written Jul 6):
> "Judge ~Jul 11 on FORWARD live PF at >=20 trades ONLY (do not anchor on the
> in-sample 1.31); kill on forward PF < 1.15."

It is now Jul 17 with **27 forward trades** (≥20) and the number is
unambiguous, not marginal:

| lane | n | net | PF |
|------|---|-----|-----|
| overall | 27 | −227 | **0.85** |
| SIDE (main regime) | 24 | −510 | **0.67** |
| BEAR | 3 | +283 | n/a (tiny) |

Daily forward PnL was choppy-negative with no trend toward the edge (Jul 8 +281,
Jul 9 −115, Jul 11 +93, Jul 12 −85, Jul 13 +98, Jul 14 +187, Jul 17 −183). The
whole positive contribution is 3 BEAR trades; its intended SIDE lane is a clear
loser. This is **mechanical pre-committed rule-application, not a fresh judgment
call** — and, crucially, it is decided on **live forward stats**, which the
backtester's non-reproducibility (the thing that paralyzed the last two
sessions) does not touch. That's why it's actionable now.

- **27 vs the generic 30-trade bar:** CLAUDE.md's generic retire bar is
  "PF < 0.9 after 30+ trades in intended regime." pdhl is at 27. The strategy's
  own ≥20-trade / PF < 1.15 criterion is more specific and was pre-committed, so
  it governs. Noted here so the early retire is defensible.
- **Execution:** flipped runtime status via `POST /api/strategy/pdhl_sweep/status`
  (the DB is the trading authority — a file edit alone would NOT stop it trading),
  **verified** `/api/strategies` shows `retired`, then synced `meta.status` in the
  file for durability. No version bump (status-only).

## Live book — vwap_revert_short holds
- **vwap_revert_short (active)**: SIDE PF **1.14** (+2113, n=230) — up from last
  session's +1415/n=206, edge intact and compounding. Group split unchanged and
  clean: **mid_alts PF 1.24** (+3107, n=204) is the real engine; large_alts
  −1301 (PF 0.68, n=52) is **frozen retired-config** bleed (dropped in v2), not
  the live book. Kill-trigger (live SIDE PF < 1.0 at n ≥ 30) **not** fired.
  → **HOLD, no edit.** (This is the one working strategy — do not touch it.)
- **BEAR lane (watch, near-trigger):** n=**26**, PF **0.84**, net −307. The
  pre-registered drop trigger is *live n ≥ 30 AND PF < 0.9 → drop BEAR
  (SIDE-only)*. n is 4 short of 30 and PF is already below 0.9; given the current
  BEAR regime it will very likely cross n ≥ 30 next session and fire. **Caveat:**
  part of this BEAR sample was tagged during the SIDE↔BEAR flicker above, so the
  labels are partly boundary-noise — when the trigger fires, sanity-check that
  the losing BEAR trades aren't just mislabeled SIDE entries before ripping BEAR
  out. Not actionable now.

## Deferred deliberately: the cap sweep (watchlist #1)
The pinned harness makes the `vwap_revert_short_capped` sweep *possible*, but
it's easy to run wrong: unless the backtest slot count is raised so "filtered" =
"baseline minus the filtered-out entries," the PF lift is just slot-reshuffle
noise — the exact artifact flagged Jul 15/16. Half-doing it produces
authoritative-looking but untrustworthy numbers. **Not started this session**
(kept scope to the one confident win). It remains the top analysis task; do it
properly (raise slots, twin runs, held-out window) or not at all.

## Watchlist / next session (ranked)
1. **BEAR drop trigger** — check first. If live BEAR n ≥ 30 & PF < 0.9, drop
   BEAR from vwap_revert_short → SIDE-only (sanity-check the flicker-tagged
   trades first, see above).
2. **Cap sweep on the pinned harness**, done properly (raised slot count, twin
   runs, held-out). Targets the APT/EIGEN repeat-short pathology. Instrument
   (`vwap_revert_short_capped`, paused, env `STRETCH_MAX`) already deployed.
3. **Re-verify past retires** that leaned on a single unpinned backtest —
   `range_fader` (retired Jul 14 on PF 0.68/0.81) most worth a pinned re-check.
4. **New-edge search (strategic, unchanged):** the book is now down to **one**
   working strategy (a thin SIDE/mid_alts short-fade), no BULL coverage, equity
   −28%. pdhl was the only other live candidate and it's gone. This is the real
   problem — a second non-correlated edge is needed. Queued: OI-confirmed flush
   (`open_interest` collecting), weekend/DoW meme effect. Best human-in-the-loop.
5. **Follow-up:** funding lookahead for pre-collection periods (from Jul 16).

## Actions taken this session
- **Retired pdhl_sweep** (7d3c660) — pre-registered forward kill (PF 0.85 < 1.15,
  n=27; SIDE lane 0.67). Runtime status flipped via API and verified; file synced.
- **No other live edits.** vwap_revert_short holds (healthy, edge growing);
  BEAR-lane trigger not yet met; cap sweep deliberately deferred to be done right.
