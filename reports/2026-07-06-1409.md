# Analyst run 2026-07-06 14:09 UTC

## Market read
- Regime SIDE most of the run, flipped BULL 14:05 UTC (ADX rising). Equity
  ~8960 -> ~9110 (recovered intra-run; still -9% lifetime paper churn under the
  human-disabled daily stop). BULL live coverage now: ema_trend_rider +
  sweep_reclaim. New pdhl_sweep (SIDE+BEAR) waits for its regime.

## Health
- collector + engine alive, zero *new* errors (one old 12:57 transient MEXC
  ticker 510 hiccup, non-repeating, not mine). Push autopulled 14:10:02Z,
  engine hot-loaded pdhl_sweep -> DB confirms `incubating v1`.
- PROCESS FIX: two multi-min backtests died to session teardown when run via
  piped SSH heredoc. Switched to detached in-container (`docker compose exec -d`
  -> /tmp file, poll) — teardown-proof. Saved as a memory for future runs.

## Concurrent session (shared clone, active NOW)
- 2nd session created whale_absorb v1 (paused, pre-gate) + retired btc_shock_fade
  at the gate (held-out 0.93), and has UNCOMMITTED edits to app/api/main.py,
  app/engine/main.py, ui/index.html in flight. I staged ONLY pdhl_sweep.py,
  fast-forward pushed clean. Left their work untouched. Staggering still advised.

## Actions (evidence)
1. CREATED pdhl_sweep v1 (incubating): fade prior-day H/L stop-hunt sweeps,
   mid_alts, SIDE+BEAR. Real-engine gate PASSED (full mechanics): 30d PF 1.31
   (n=144, win 54%, +0.89%/day); BEAR 1.34 + SIDE 1.28 both clear; halves
   h1 1.15 / h2 1.40; 14d 1.36. Weak spot: last-7d PF 1.02 (n=34, noisy week)
   -> incubation decides. Adds LONG mid_alts coverage no live strategy had.

## Ideas tested and killed (event studies)
- PDH/PDL sweep, loose trigger: coin-flip (0.1-ATR poke, PF ~0.8, ~1.4x/sym/day).
  Edge lives ONLY in SIGNIFICANT sweeps — PF rises monotonically with poke depth
  x volume. DROPPED by the group x regime split: large_alts (0.87), memes
  (h1 0.53/h2 1.38 unstable), BULL (0.85). Kept mid_alts SIDE+BEAR (the win).

## No-touch (discipline: n<20 or wrong regime)
- No RETIRE/TUNE/PROMOTE/LOCK triggered. vwap_revert_short n=16 (nearing judge),
  range_fader n=9, ema_trend_rider n=10 (large_alts drag), sweep_reclaim n=3.

## Backlog (hypothesis status)
- [testing] pdhl_sweep incubation — judge ~Jul 11 (>=5d & >=20 trades). Watch the
  7d weak spot; kill if incubation PF < 1.15.
- [testing] sweep_reclaim incubation — judge ~Jul 10.
- [queued] OI-confirmed flush — open_interest still only ~1-2d deep (~Jul 19).
- [prepared, deferred] weekend/day-of-week meme effect on 180d 1h data
  (es_dow.py ready). Held back — one solid creation this run beats two marginal
  ones. Run next session.
- [dead] flush_reversal, funding fade (both sides), squeeze_ride, dispersion_fade,
  pump_fade, breakout_momentum, BTC lead-lag, settlement drift, bear-onset. Don't
  revive without new data/mechanism.
