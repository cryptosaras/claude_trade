# Analyst run 2026-08-04 ~14:30 UTC

## PRE-REGISTRATION (written and committed BEFORE any census, probe or backtest was run)

Scheduled run, no human present. Yesterday's watchlist had *"pre-register the
truncated-bar fix"* at #1 and *"quantify the damage: re-run the census on
truncated bars"* at #2. **I am inverting that order, and the reason is the whole
design of this session.**

Aug 3 named three candidate fixes: (a) require 15 constituent 1m candles before
a 15m bar counts as closed; (b) offset the decision tick past the collector's
~87 s refresh; (c) cut the collector cycle so refresh < tick. **The trade-off
between them is undecidable today**, because each one's cost is denominated in a
quantity nobody has measured: (a) removes signals — how many, and were they the
good ones? (b) adds up to ~90 s of entry lag — worth how much? (c) stresses the
component already throwing MEXC `510`s. A session that produces only a design
document produces no evidence, and would have to guess at all three numbers.

**So this session measures the wedge, and lets the number choose the design.**
The fix gets pre-registered next session with a measurement in hand.

### Authority — what this session may and may not do

`CLAUDE.md`'s analysis loop authorizes strategy, param, group, regime, universe
and config changes. It does **not** authorize changing the engine's decision
loop, the tick interval, or the collector cadence. Those are the shared substrate
every stored result in this repo depends on, there is one live instance, and it
cannot be A/B'd. Aug 3 already pre-committed that the fix needs its own
pre-registration. **That line holds today: no engine change, measurement only.**

### Disclosure: what I had already seen when writing this

Health state only, from `/api/report` before drafting: equity **6,129.27
(−38.71%)**, **0 open positions**, regime **SIDE** (ADX 21.3, BTC 63,850),
collector and engine alive, `whale_absorb` now at **n=12** live trades (was 10),
`avg_daily_pct` **−0.217**. The two new closes are `ORDI_USDT` **−4.55** and
`INJ_USDT` **+39.52**, both *bounce window over*. Five MEXC `510`s now visible in
the event feed (Aug 1 19:31, Aug 2 12:24, Aug 3 20:09, Aug 4 05:56, Aug 4 11:16),
and the regime label flipped BEAR↔SIDE nine times in the last 24 h.

**No census, probe or backtest had been run when this section was written.**

### The mechanism being measured (restated, because the design depends on it)

`_closed_bars` ([whale_absorb.py:60](strategies/whale_absorb.py:60)) drops the
newest resampled bar only when `df.index[-1] + 15min > ctx.now`. It tests **when
the bar's window ended**, never **whether its 15 minutes of data arrived**. The
collector refreshes each symbol about every 87 s; the engine ticks every 20 s and
was measured entering within 0.00–0.67 min of the bar boundary. So at the tick
where bar `T` first counts as closed, bar `T` is typically missing its **last 1–2
minutes** while every earlier bar is long since complete.

That asymmetry is what makes the measurement tractable: **only the signal bar is
truncated.** Prior bars — which supply `avg20` volume, the 12 h low, the 6 h
reference close, and the ATR history — are complete.

### What is measured — the wedge

For truncation depth `m` (the signal bar missing its last `m` minutes), rebuild
bar `i` from its first `15 − m` 1m candles, leave all prior bars complete, and
re-evaluate `whale_absorb`'s entry conjunction exactly as `should_enter` does.

**ATR is recomputed on the truncated series, not carried over from complete
data.** This is the exact error that forced the Aug 3 correction, and it is
carried in as a standing check. `ind.atr` is a Wilder EMA, so with only bar `i`
modified the correct value is `ATR_i(m) = ATR_{i−1}·(13/14) + TR_i(m)/14`, with
`TR_i(m)` built from the truncated high/low against the **complete** prior close.

**Depths, pre-declared:**

- **`m = 0`** — the specified strategy (complete bars). The baseline.
- **`m = 1` and `m = 2`** — the primary cells, matching the 1.2–1.7 min measured
  staleness and the `sl`-channel bounds established Aug 3 (`ORDI` 527 exactly 1
  missing minute, `JTO` 1–2, `UNI` 1–5).
- **union over `m = 1..5`** — the upper bound. `_closed_bars` re-admits bar `T`
  on every 20 s tick for the following 15 minutes, so a signal reachable at *any*
  tick before the bar completes is reachable by the engine. This is a ceiling,
  not an estimate, and is labelled as one.

**Three classes are counted, not one number:**

| class | meaning |
|---|---|
| **TRUNC-ONLY** | fires under truncation, not on the complete bar — *the engine takes a trade its own rules reject* |
| **BOTH** | fires either way |
| **COMPLETE-ONLY** | fires on the complete bar, not under truncation — *the engine misses a trade its rules require* |

**The wedge is the difference in mean net %/trade between the engine's realized
set and the specified set**, and it is reported **decomposed into two channels**,
because conflating them would be uninterpretable:

- **W1 — signal-set effect only.** Both sets priced at the complete-bar close.
  Isolates *which trades are taken*.
- **W2 — full engine view.** The truncated set priced at the truncated close (the
  engine's `ctx.price`), with its own truncation-derived SL and TP; the specified
  set priced at the complete close. This is what actually happens.

Forward paths start at the bar boundary `T+15` in both cases, on 1m candles.
Three exit variants on each identical signal set, as Aug 2: **RAW** (8 h hold),
**STOP** (2.5 ATR), **STOP+TP** (2.5 ATR + 1.5 R — *the deployed config*). All
net of **0.16%** read from `config/settings.yaml`. **No fee or slippage number is
touched.**

### Window and universe, pinned now

**Signal bars with close in [2026-07-06T00:00Z, 2026-08-03T21:00Z].** The 8 h
horizon puts the last exit at 2026-08-04T05:00Z, inside available data.

**Both the truncated and the complete census run inside this session, on this
window.** I do **not** diff against Aug 2's published −0.356% / n=285: two days of
collector backfill on newly discovered symbols can change an old window, and this
repo's own standing check forbids arithmetic on a previous report's number.

**Universe, both pre-declared so neither can be chosen after the fact:**

- **Primary: all collected symbols with sufficient 1m history.** Truncation is a
  property of the data pipeline, not of the trading universe, and the wedge needs
  power — Aug 2 got n=285 here against n=172 on group symbols.
- **Secondary: the ~72 group symbols** (non-majors), which is what the live engine
  actually loads and trades.

### Power check, run and reported BEFORE any null is banked

Aug 3's lesson was a pre-registered test whose negative result was guaranteed in
advance, because the variable was saturated at 100%. **The base rate is therefore
checked first this time:** at `m = 1`, over all evaluable bars, what fraction have
**any** of the five clauses change truth value? If that fraction is ~0, the
instrument has no power and every downstream null is void — and I will say so
instead of banking it.

### Void condition

The reconstruction must, at the `sl`-consistent depths Aug 3 established on two
independent channels:

- **produce `UNI` (526) and `JTO` (530) as signals under truncation**, and
- **not produce them on the complete bar**, and
- **produce `ORDI` (527) under truncation.**

If it cannot reproduce three cases that two channels already nailed down, it is
not measuring the live engine, and this session reports a **tooling failure, not
a result**. (Third session running: the void condition has been worth more than
the predictions both previous times.)

### Pre-committed predictions (two only — padding the list has cost twice)

1. **Truncation is net signal-manufacturing at `m = 1`: `TRUNC-ONLY` >
   `COMPLETE-ONLY`.** The mechanism is directional — `whale_absorb` keys on
   *narrow range* and dropping minutes can only narrow a bar, never widen it — so
   this is falsifiable and the direction is not free (the volume clause moves the
   other way: a truncated bar has *less* volume, so `vol ≥ 2.5× avg20` gets
   harder). Which of the two dominates is the actual open question.
2. **The wedge is small relative to the effects this lab already argues about:
   `|W1 mean %/trade, STOP+TP, m=1| < 0.30 pp`.** That yardstick is the size of
   the stop's protective effect measured Aug 2 (+0.295 pp). Aug 3's counterweight
   pointed this way — the two known artifact entries were `UNI` −69.95 and `JTO`
   +83.32, net **+13.37** — so if the wedge comes in *larger* than 0.30 pp, the
   defect is not merely a provenance problem and that becomes the finding.

### Supplementary measurements (cheap, declared here so they are not post-hoc)

- **S1 — has the pipeline degraded?** Re-measure the collector cycle against Aug
  3's **72.2 s**, and re-take the staleness snapshot (3 snapshots 20 s apart,
  20 group symbols) against Aug 3's **1.2–1.7 min mean**. Five `510`s in four days
  after eight clean sessions is a candidate cause. **If the cycle lengthened,
  truncation depth is growing and fix design (c) is more dangerous, not less** —
  which directly ranks the designs.
- **S2 — extend the Aug 3 `sl`-channel table from 10 entries to 12**, using the
  committed `sl_discriminator.py` unchanged, on the two new entries `535` (`ORDI`)
  and `536` (`INJ`).

### Pre-committed regardless of outcome

- **No engine, collector, tick-interval or `_closed_bars` change.** The fix is not
  this session's to make (see Authority above).
- **No status change on any strategy**, in file or DB. The Aug 3 recommendation to
  retire `whale_absorb` is **restated, not executed** — retiring it empties the
  book, which is a human call.
- **No param, `meta.regimes`, group, universe or config edit.**
- **R1 not evaluated**: n=12 of the required 20.
- **No change to the `n ≥ 20` gate** (human call, Jul 31), **no unpausing of
  `vwap_revert_short`** (human call, Jul 21), **no `open_interest` work**
  (mid-August), **no re-opening of `pump_fade`** (closed Jul 29) or
  `whale_absorb_runner` (rejected Aug 1), **no re-tuning of the stop** (declined
  Aug 2).

### Standing checks carried in

- Any run with a non-empty `.err` is void, not a result.
- Twinned runs, byte-identical output, before any number is used.
- Filter `tf='1m'` on any candle query.
- Per-symbol concentration reported on any edge claimed.
- Standard errors on every cell; nothing inside ~2 SE is called a finding.
- Live PnL/PF/exit-mix pulled from the DB, not carried forward as arithmetic on a
  previous report's number.
- **ATR recomputed on the truncated series** (the Aug 3 correction).
- Long runs detached in-container (`docker exec -d`), not through a piped SSH
  session.

### Expected outcome

Most likely: prediction 1 lands (truncation manufactures more signals than it
destroys, because the range clause is the binding one — Aug 2 Finding 4 showed
both live misses failed on exactly that clause), prediction 2 lands (the wedge is
real but small), and the session's output is **the first measured size of the
live-vs-backtest wedge** plus a ranking of the three fix designs by their measured
cost. The more valuable branch is prediction 2 failing: a large wedge would mean
every live-evidence retirement in this repo's history was made against a
mis-measured baseline.

---

# OUTCOME

*(to be written after the measurements run)*
