# Analyst run 2026-07-05 15:29 UTC (first run)

## Market read
- Regime BULL (conf 1.0); flip rate normal (~1 per 19h last week, same as prior 2 weeks).
- BTC ~62.7k. Equity 9733 (-2.67%): 3 closed trades, all SIDE-regime losers from launch
  morning; fees were 30-45% of each net loss — tight stops churn fees. 4 open BULL longs.

## Health
- Collector + engine alive, no error events. Engine restarts in the log were autopull rebuilds.
- Watch: range_fader and vwap_revert_short shorted TRX_USDT the same second/price/size —
  two SIDE faders share one thesis; group cap (2/group) permitted the stack. n=1, watch only.

## Actions
1. NEW FEED: `open_interest` table — 5-min holdVol snapshots, all ~68 collected symbols,
   zero extra API calls (field already in the ticker payload). Verified: 1967 rows/68 syms
   in first 2.5h. Unlocks OI-surge/divergence strategies once history accumulates.
2. NEW STRATEGY `sweep_reclaim` v3 (incubating): 5m wick sweeps an 11h extreme and closes
   back inside on >=1.5x volume -> fade toward the range; BULL longs / BEAR shorts;
   mid_alts + memes; SL beyond wick (0.4%..2.5% guard), TP 2R, ~1-2h holds.
   Validation (symbol-split holdout designated before first run):
   - Dev 21d (NEAR/ARB/SUI/TIA/DOGE/WIF/FLOKI): v1 PF 0.72 — SIDE regime was the entire
     loss (PF 0.55, n=58); BULL 1.42 (n=12), BEAR 1.07 (n=17). v2 = drop SIDE, only change.
   - HELD-OUT 21d (APT/OP/SEI/INJ/SHIB/PEPE/BONK): PF 1.61, +13.5%, n=37, maxDD 8.0% — PASS.
   - Dev 21d v2: PF 1.07, +1.4%, n=28 — thin but positive.
   - Recency slice (7d, all symbols): PF 0.46, n=28 — one bad week; the held-out 21d PASS
     already contains that week. Incubation arbitrates: promote 5d/20tr/PF>=1.15, else retire.

## Ideas tested and killed
- sweep_reclaim trading SIDE: killed (PF 0.55, n=58, dev 21d).
- "Bad recent week = regime whipsaw": killed (flip rates equal across windows).

## No-touch decisions
- All 5 seed strategies: n<=3 trades each, no tuning possible (sample-starved rule -> created
  instead). Judge seeds from ~Jul 10 when n>=20 accumulates.

## Backlog
- [testing] sweep_reclaim incubation — decide at >=20 trades / >=5 days (from Jul 10).
- [queued] Regime-age filter (v4 hypothesis) IF incubation losses cluster near regime flips —
  mechanism-based, must be tested on data that doesn't exist yet, not on the known bad week.
- [queued] Funding-history replay in backtester: app/backtest/main.py applies TODAY's funding
  snapshot to the whole window — funding-GATED entries (funding_skew_fade!) are not honestly
  backtestable. `funding` table fills hourly since today; wire rate-as-of-t when >=2wk exists.
- [queued] OI-surge/divergence strategies — need >=2wk of open_interest history (from ~Jul 19).
- [queued] Funding-settlement drift (00/08/16 UTC) — needs funding replay first.
- [queued] BTC lead-lag on alt 1m bars — needs cross-symbol ctx (engine change); propose only
  with a concrete design next run.
- [note] Fee drag rule of thumb: prefer setups with SL >= 0.4% of price (sweep_reclaim's
  min_sl_pct exists for this; seeds have no such guard — revisit when their samples mature).
