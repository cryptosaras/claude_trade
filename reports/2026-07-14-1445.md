# Analyst run 2026-07-14 14:45 UTC

## Market read
- Regime **SIDE** (conf 0.84, ADX 23.8) as of ~10:02Z today, after a stint in
  BEAR (BULL on Jul 11 → BEAR → SIDE now). BTC ~64.0k, ema_slope_24h slightly
  negative (−0.0022). Chop with a mild down-drift; mean-reversion tape, but with
  a nasty twist this window: several mid_alts (APT, EIGEN) are in sharp local
  rips that repeatedly run the short-fade stops.
- Equity **7016** (−29.8% lifetime), down from 8786 on Jul 11 — a **−1770**
  drawdown over 3 days. Calendar-today PnL −1704 on 33 closed trades; 4 open.

## Health
- collector + engine alive, no error/crash events. Autopull converged 314f1e1.
  Regime change BEAR→SIDE logged 10:02Z; the book woke up as SIDE strategies
  re-engaged — and immediately started bleeding (see below).

## Drawdown attribution (windows stated explicitly)
- **range_fader is the cause.** Last 24h (rolling, closed trades): range_fader
  −945, vwap_revert_short −267, pdhl_sweep +386 (net closed ≈ −826; remainder of
  the −1704 calendar-today figure is open-position MTM + window edges).
- Over 7d (closed): range_fader large_alts n=71 **PF 0.68, −1707**, wr 25%.

## Action: RETIRED range_fader
- Evidence, its **exact intended lane** (SIDE / large_alts): lifetime n=85
  **PF 0.81, −1172**; forward-7d n=71 **PF 0.68, −1707**, wr 25%. Plus legacy
  majors n=21 PF 0.76 (−414). Lifetime total ≈ −1586 over 106 trades.
- The Jul-10 v2 promotion (large_alts n=45 PF 1.21, +654) is **fully invalidated
  by forward evidence** — that sample was overfit/lucky; forward it collapsed to
  0.68. PF<0.9 with n≫30 in the intended regime → retire rule, unambiguous. It
  is the single biggest contributor to the Jul 11→14 drawdown.
- Mechanic failure: fading the upper Bollinger band + RSI>72 in this tape just
  shorts strength that keeps grinding (wr 25%) — the same failure mode as VWAP
  fade in BULL. The band-touch edge does not survive a trending-within-SIDE tape.
- Disposition: DB status → retired first (entries stopped 14:4xZ, bleeding
  halted), file `meta.status` synced to `retired` with the numbers in-docstring.
  Open positions remain engine-managed (status gates entries, not exits).
- **large_alts trading coverage → zero.** Accepted: flat beats a PF-0.68 lane.

## pdhl_sweep (incubating) — explicit disposition: HOLD incubating, do NOT promote
- Crossed n≥20 this window (the pre-registered revisit trigger from Jul 11):
  25 total trades, 8 days incubation (first exit Jul 6).
- Forward-7d looks strong (n=21, PF 1.39, +366, wr 57%; last 2 calendar days
  +187/+382). **But that is a rolling-window artifact:** its positivity depends
  on the boundary excluding the early Jul-7 −418. Full-incubation calendar-day
  sum (Jul 6–14, same v1, no re-tune) = **−45. Net NEGATIVE.**
- Gate distinction (was conflating two bars): the Jul-11 trigger was a
  **kill-check** — "judge forward PF, kill if <1.15." 1.39 clears that, so
  **keep it, don't kill.** The **promotion** gate is stricter — "promote only if
  net-positive over the incubation window" — and lifetime −45 **fails** it.
- Promotion buys nothing anyway: incubating strategies already trade live-paper;
  the label is the only difference. So holding costs zero trades and lets the
  evidence become unambiguous. **Re-judge when net-positive over the full window
  with margin** (not a boundary-sensitive rolling slice).

## Live actives after this run
- **vwap_revert_short** v2 (SIDE/BEAR, mid_alts) — HOLD, unchanged. Still the
  engine: 7d mid_alts n=130 **PF 1.15, +1291**, wr 36%. Last 2d breakeven
  (n=70, PF 1.01, +59) — a rough patch, not a break. Cause is visible in the
  event log: it repeatedly shorted **APT** and **EIGEN** (both 5–6 ATR above
  VWAP, RSI 70+) and got stopped 4–6× each as they kept ripping, re-entering at
  ever-higher prices; winners like DYDX (+279 TP) offset it to breakeven.
  Watchlist item below.
- pdhl_sweep v1 (incubating, mid_alts) — held (above).

## Portfolio state
- Trading: vwap_revert_short (active, mid_alts, SIDE/BEAR), pdhl_sweep
  (incubating, mid_alts, SIDE/BEAR). Book is now **mid_alts-only** mean
  reversion. large_alts and BULL coverage both zero — deliberate; every idea
  tried in those lanes loses on this tape.
- Retired/paused (untouched): range_fader (now), vwap_revert_short_bull,
  ema_trend_rider, sweep_reclaim, breakout_momentum, whale_absorb, btc_shock_fade.

## Watchlist / next session
- **APT/EIGEN repeat-stop pathology in vwap_revert_short.** The strategy is net
  fine, but it burns money re-shorting a single coin that's in a vertical rip,
  taking 4–6 consecutive stops on the same symbol. Candidate fix to TEST (full
  gate, do not hot-edit the live engine on a hunch): a per-symbol cooldown —
  after N consecutive stops on a symbol, stop re-entering it for M hours. Would
  need a symbol-loss-streak signal the strategy can see. Backtest first; only
  worth it if it lifts mid_alts PF without cutting the DYDX-type winners.
- pdhl_sweep: re-judge for promotion **only** when full-incubation net turns
  positive with margin. Until then it trades as incubating; that's fine.
- Goal tracking: vwap_revert_short +0.898%/day blended (224 trades) vs 2%
  target; mid_alts-only historically ~1.5×. Binding constraint remains the tape
  — the runaway-mid_alt rips are eating the fade edge this week.
- Still queued (older): weekend/day-of-week meme effect (es_dow.py, Jul 6);
  OI-confirmed flush (open_interest data ~Jul 19; the open_interest table exists
  and is collecting — worth checking coverage next session).
