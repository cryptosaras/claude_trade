# Analyst run 2026-07-28 15:05 UTC

## PRE-REGISTRATION (written and committed BEFORE any screen number was known)

This session executes **watchlist item 3** from Jul 27: re-run the book screen
with the new gross columns to separate *coin flips* from *thin edges wearing too
much toll*. One design change from how that item was written, and one addition.

### Change: the screen runs on window B only, not window A

Item 3 said "re-run the screen". Finding 6 of the Jul 27 report established that
**window A (Jun 9–25) is in-sample** for the ~11 strategies authored Jul 5–6 —
their 30-day creation studies span roughly Jun 6 → Jul 6. A gross screen there
would answer "did the tuning stick", not "is there a signal". **Window B
(`--days 14 --end 2026-07-27T00:00`, Jul 13–27) is the only clean window** —
after every parameter freeze in the book. The classification is only meaningful
on clean tape, so window A is not re-run.

### Addition: gross PF alone would misclassify the high-turnover strategies

`gross_pnl` is computed from the filled entry/exit prices, so **slippage
(0.03%/side) is already inside it**; only fees are outside. Gross PF therefore
charges each strategy 0.06% of notional per round trip, in proportion to how
often it trades. Four strategies in this book turn over very fast —
`dispersion_fade` (n=517 on window A, 0.4h holds), `breakout_momentum` (378,
0.5h), `vwap_revert_short_capped` (366, 0.8h), `pump_fade` (144). For those, a
gross PF near 1.0 is **not** proof of "no signal to rescue"; it is equally
consistent with a real signal buried under slippage from over-trading — the
exact category watchlist item 2 wants to surface. Publishing a two-bucket table
off gross PF alone would hand the next session a wrong verdict on precisely the
strategies most likely to respond to cost-aware re-tuning.

So the classification is **three-way**, using a per-trade cost decomposition
derived from `config/settings.yaml` (`fee_taker` 0.0005, `slippage` 0.0003 — both
untouched, per the honesty rules):

- fees round trip = `0.001 × notional` ⇒ **`avg notional ≈ fees_per_trade × 1000`**
- slippage round trip = `0.0006 × notional` ⇒ **`slip/trade ≈ 0.6 × fees_per_trade`**
- **`raw edge/trade = gross_edge_per_trade + 0.6 × fees_per_trade`** (signal before *any* cost)
- **total cost/trade = `1.6 × fees_per_trade`**

| bucket | test | meaning | re-tuning prospect |
|---|---|---|---|
| **A — no raw edge** | raw edge/trade ≤ 0 | directionally wrong before any cost | none; retirement final |
| **B — raw edge, killed by turnover** | raw edge > 0, gross PF ≈ 1.0, short holds / high n | signal exists, slippage eats it | trade less, be more selective |
| **C — raw edge, killed by fees** | gross PF > 1.05, net PF < gross | signal survives slippage, fees finish it | size differently (wider stops → smaller notional) |

Note ahead of the results: `range_fader` (n=108, 2.7h holds) and `sweep_reclaim`
(n=35, 1.7h) are **not** high-turnover, so whatever this screen says, it does not
reverse the Jul 27 vindication of those two retirements. This is a refinement of
that finding, not a contradiction of it.

### Reproducibility: what a deviation will and will not mean

Window B was last run on Jul 27; funding backfill has fired **4 times on Jul 28
alone**. My own memory note says a comparison spanning days is exactly where
backfill legitimately moves a pinned window. Recording the discriminator **now**:

- **Window-B fingerprint at session start (15:10Z):** 74 group symbols,
  **171 distinct symbols** with candles in Jul 13–27, **3,037,271 candle rows**.
  Re-measured at session end.
- Known window-B values to reproduce: **`pdhl_sweep` n=59 / PF 1.07**;
  **`whale_absorb` n=26 / PF 1.32**.
- If those deviate **and** the fingerprint changed, it is **backfill, not
  nondeterminism**, and it is not investigated further. If they deviate and the
  fingerprint is identical, that *is* a methodology problem and takes priority
  over the screen.
- The three-way classification is robust to ±0.05 PF, which is why single-shot
  triage is adequate. **Twins are run only for a number quoted as a decision
  input.** Jul 27 verified single-shot triage reproduced exactly.

### Pre-registered scope

**The expected and most likely correct outcome of this session is zero status
changes.** `whale_absorb` has n=2 live — no verdict is possible or attempted.
Nothing else is eligible. This session's deliverable is the classification table
and the report. If a status changes, it will be because something unforeseen
turned up, and the reason will be stated explicitly.

---

## PRE-REGISTRATION ADDENDUM — one derived-parameter test on `pdhl_sweep`

Also written before the run. Added mid-session because the cost algebra below
(Finding 1) produced a **falsifiable prediction with no free parameters**, which
is worth more than the screen it came out of.

### The prediction

`pdhl_sweep` sizes at 1% risk over its stop distance and exits at `tp_r = 1.5`.
So for a trade whose stop sits `d` (as a fraction of price) away, the winner is
`+1.5d` and the loser `−d`, and at win rate `p` its edge yield is
**`d · (2.5p − 1)`** against a hard round-trip cost of **0.16% of notional**.
Solving for the stop distance at which a trade can clear its own costs:

| win rate `p` | break-even stop distance |
|---|---|
| 0.48 | 0.800% |
| 0.50 | 0.640% |
| **0.508** (its window-B rate) | **0.593%** |
| 0.52 | 0.533% |
| 0.55 | 0.427% |

**Its `min_sl_pct` floor is 0.400%.** At its own observed win rate, *every trade
it takes with a stop between 0.40% and 0.59% is structurally unprofitable* —
not because the signal is wrong, but because the geometry cannot pay the toll.
The docstring shows the author saw the mechanism (*"skip if stop closer than
0.4% (fees dominate R)"*) and set the floor roughly a third too low.

**Pre-registered prediction:** raising `min_sl_pct` from `0.004` to `0.006`
raises window-B net PF above the current 1.07. If PF does **not** rise, the
assumption that win rate is independent of stop distance is false, and I will
say so — that is the interesting outcome, not a failure to report.

### Why this is not tuning on my only clean window

The value **0.006 is derived, not searched**: it is `0.0016/(2.5p−1)` at the
observed `p`, rounded up. There is exactly one candidate and no selection over
window B. That is categorically different from the group-narrowing I refused
last session (which would have picked the winner *out of* window B results).

Honest limits, fixed now:
- I will also run `0.008` **as information only**. It is *not* a decision input
  and no promotion may cite it. Reporting two values is mild selection and I am
  naming it rather than hiding it.
- **No status changes regardless of outcome.** `pdhl_sweep` stays `retired`.
  A pass makes it a *candidate* for a human-attended re-tune with a fresh
  held-out window, which is watchlist item 2 and explicitly a human call.
- If `n` falls below 20 the test is **uninformative**, not negative, and will be
  reported as such — raising the floor necessarily discards trades.

---

## Market read — the dormancy ended, and the regime finally committed

- **Regime BEAR, confidence 1.00, ADX 33.3, ema_slope_24h −0.00351**, BTC 63,394
  (15:18Z). BEAR since **Jul 28 06:03Z** — 9h and holding.
- This is the **first decisive directional label in weeks**, and it matters
  because Jul 27's report flagged the opposite condition: ADX 35.8 with an
  EMA200 slope of +0.00025, a quarter of the BULL threshold, tie-breaking to
  SIDE at trend onset. The slope has now caught up and inverted (−0.00351,
  3.5× the threshold in the BEAR direction), so label and ADX finally agree.
- **Jul 27 was a flicker storm**: SIDE→BULL→SIDE→BULL→SIDE→BULL→SIDE, five flips
  in ten hours, with BULL segments lasting 2.5h, 2.2h and 3.1h. That is the
  tape `whale_absorb` was switched on into.
- **Volatility watch (Jul 27 watchlist item 5):** the vol-compression story
  predicted suppressed results. BTC has fallen 65,438 → 63,394 (−3.1%) in 33h,
  which is the first real range expansion in the 8-week record. Not re-measured
  formally this session — the box was saturated by the screen — but the
  direction is worth noting against a prediction of continued compression.

## Health — no action

- Collector and engine both alive; server time 15:20:15Z, regime tick 15:18:50Z
  — 85s old, fresh.
- Equity **6,294.19 (−37.06%)**, 0 open positions.
- **No new MEXC 510 since Jul 24 00:54Z** — unchanged for a 4th session.
  Monitor only.
- VPS 2 cores / 3.8 GB. Backtest concurrency capped at 2; every run verified by
  non-empty JSON **and** 0-byte `.err` (the backtester passes no `on_event`, so
  a crashing strategy would otherwise return `trades: 0` silently).

## `whale_absorb` — 2 live trades, both losers, and NO verdict is drawn

It opened its first two live trades since promotion, both long, both stopped
out: `ACT_USDT` −68.70 and `UNI_USDT` −69.95, **−138.65 total**, which is the
entire equity change since Jul 27.

**n=2. Jul 27 watchlist item 1 said do not judge it early, and that stands
without qualification** — the gate wants ≥20 trades and ≥5 days, and at ~1.9
trades/day with contested slots that is 2–4 weeks. Nothing is done about it and
nothing should be.

One observation worth recording as a *hypothesis*, not a finding: **both entries
landed inside BULL labels that lasted under 2.5h** during the Jul 27 flicker
storm. Its window-B BULL evidence (n=23 @ PF 1.35) came from a very different
tape — of window B's 129 BULL hours, **124.5 (96.5%) were in segments longer
than 4 hours**, across only 6 such segments. So its one out-of-sample BULL
result was earned in sustained trends, and its two live losses came from
transient labels. That distinction is testable and cheap (bucket its window-B
entries by the dwell time of the label they opened in) but was not run this
session. **At n=2 it is a story, and stories at n=2 are how this repo has
previously misled itself.**

The genuinely relevant news is the regime: **its incubation effectively starts
now.** BEAR is where its window-A sample was PF 3.04 (n=30), it has been idle
through SIDE by design, and BEAR has been stable for 9 hours. The −138.65 is
not the experiment.

---

## Finding 2 — the pinned window held across a full day and four funding backfills

The pre-registration named this in advance so a deviation could not become a
session-eating investigation. It did not deviate. `whale_absorb` re-run on
`--days 14 --end 2026-07-27T00:00`, 24 hours after Jul 27's twinned pair:

| | Jul 27 | **Jul 28 re-run** |
|---|---|---|
| trades | 26 | **26** |
| net PF | 1.32 | **1.32** |
| net | +295.99 | **+295.99** |
| win rate | 50.0% | **50.0%** |
| max DD | 4.55% | **4.55%** |
| avg hold | 5.45h | **5.45h** |
| by regime | BULL 23 @1.35 · BEAR 3 @0.53 | **identical** |
| by group | mid 17 @1.41 · large 7 @0.63 · memes 2 | **identical** |

**Every field reproduces exactly**, including the group splits. Jul 27 showed a
pinned window holding across 31 minutes; this extends it to **24+ hours spanning
four `funding history backfilled` events** (Jul 28 at 01:27, 08:10, 09:22,
12:39Z). The memory note's caveat — that backfill *can* legitimately move an old
window — remains true in principle and has now failed to bite twice.

Practical consequence: **single-shot pinned runs are trustworthy in this repo**,
and the twin-run requirement can be reserved for genuinely marginal decision
numbers rather than applied to every screen. That is worth roughly half the
runtime of a session like this one.

---

## Finding 1 — THE HEADLINE: there is exactly one number that decides whether a strategy can pay its costs, and it is scale-free

This session set out to use gross PF to classify the book, discovered gross PF
is the wrong instrument, and derived the right one. **This supersedes the
two-bucket/three-bucket framing in my own pre-registration above** — reported as
a correction rather than quietly substituted.

### The algebra, verified against `app/engine/account.py` rather than assumed

`Broker.open` fills at `price·(1+s)` for a long, `Broker.close` at
`price·(1−s)`, with `s = slippage = 0.0003`; fees are `notional · 0.0005` on
each leg. So for a trade with entry notional `N` and underlying price move `r`
(the raw signal, before any cost):

```
gross ≈ N·(r − 0.0006)          slippage is INSIDE gross, both legs
net   ≈ N·(r − 0.0006 − 0.0010) = N·(r − 0.0016)
```

Summing over trades, the book is net-profitable **iff**

> **the notional-weighted mean raw move `r` exceeds 0.16%.**

Call that weighted mean the **edge yield**. It is recoverable from the
backtester's existing output, since `fees_per_trade = 0.001 × notional`:

```
edge yield = (gross_edge_per_trade + 0.6 × fees_per_trade) / (1000 × fees_per_trade)
```

**Trade count, notional, and leverage all cancel** — they scale edge and cost by
exactly the same factor. Funding is immaterial and favourable (+13.43 collected
across 526 trades ≈ 0.0002% of notional) and is excluded.

### It rank-orders the live book perfectly — 6 of 6, monotone

All closed live positions, edge yield against the fixed 0.16% hurdle:

| strategy | n | **edge yield** | **× hurdle** | net PF | gross PF | avg notional | hold |
|---|---|---|---|---|---|---|---|
| `vwap_revert_short` | 304 | **0.1836%** | **1.15×** | **1.05** | 1.31 | 14,378 | 1.45h |
| `pdhl_sweep` | 27 | 0.0849% | 0.53× | 0.85 | 1.05 | 11,152 | 1.08h |
| `sweep_reclaim` | 35 | 0.0609% | 0.38× | 0.78 | 1.00 | 15,967 | 1.36h |
| `range_fader` | 108 | 0.0594% | 0.37× | 0.78 | 1.00 | 16,428 | 2.02h |
| `ema_trend_rider` | 42 | −0.0528% | −0.33× | 0.43 | 0.62 | 17,037 | 1.15h |
| `breakout_momentum` | 8 | −0.0979% | −0.61× | 0.31 | 0.48 | 25,600 | 0.54h |
| **whole book** | **526** | **0.1130%** | **0.71×** | **0.90** | 1.14 | 15,125 | — |

Edge yield orders net PF **monotonically across all six**, and the 1.0× hurdle
lands exactly where the sign flips: `vwap_revert_short` is the only strategy
above it and the only one with net PF > 1. The book's 0.71× reproduces Jul 27's
"signal earns ~70% of its toll" from an independent direction.

The same holds on out-of-sample backtests (window B) — three more observations,
still monotone:

| | edge yield | × hurdle | net PF (window B) |
|---|---|---|---|
| `whale_absorb` | 0.343% | 2.14× | 1.32 |
| `pdhl_sweep` | 0.190% | 1.19× | 1.07 |
| `vwap_revert_short` (v3) | 0.140% | 0.88× | 0.95 |

**Nine independent (strategy, window) observations, no inversions.**

And the version split that cost this repo three sessions falls out cleanly:

| `vwap_revert_short` era | n | edge yield | × hurdle | net |
|---|---|---|---|---|
| v1+v2 | 279 | **0.2047%** | **1.28×** | +1,864 |
| v3 | 25 | −0.2207% | −1.38× | −823 |

### Three corrections this forces — two of them to my own prior claims

1. **My pre-registration's "bucket B" is incoherent and I am striking it.** I
   wrote that high-turnover strategies might have raw edge "killed by turnover",
   fixable by trading less. Wrong: slippage is proportional to notional *per
   round trip*, exactly like fees, so cutting frequency scales edge and cost
   identically and **leaves edge yield unchanged**. Trading less often is not a
   lever at all. Only *selectivity* helps, and only insofar as the trades it
   drops had below-average `r`. The concern that gross PF would misclassify the
   fast strategies was correct; the fix is a better statistic, not a third
   bucket.

2. **Jul 27's mechanism for why `whale_absorb` beats `pdhl_sweep` is wrong.**
   That report said its wide stops mean "smaller notional at fixed 1% risk — so
   it pays less than half the fee load **for the same signal**". Their gross
   dollars per trade *are* near-identical (16.87 vs 17.24), which is what
   invited the reading. But per unit notional the signals are not the same:
   **0.343% vs 0.190%**. `whale_absorb`'s signal is 1.8× stronger; the matching
   dollar figures are an artifact of its 5,960 notional against `pdhl_sweep`'s
   13,280. Scaling `whale_absorb` up to the same notional leaves its PF at 1.32.
   **The lever is the size of the captured move, never the size of the position.**

3. **`range_fader` and `sweep_reclaim` are not literally coin flips** — they
   carry a small real raw edge (0.059%, 0.061%) that gross PF 1.00 concealed.
   This does **not** reopen their retirements; it closes them harder. At 0.37×
   the hurdle they would need to nearly *triple* the move they capture, which no
   sizing, frequency or leverage change can deliver. Jul 27's verdict was right;
   its stated reasoning was wrong in a way that mattered, because "coin flip"
   implied nothing was there, while "37% of the hurdle" says exactly how far
   away it is.

### The gap, stated two ways — only one of them is honest about fragility

The book needs +0.047 points of edge yield (0.113% → 0.16%). By realized exit
geometry:

| exit reason | n | edge yield | avg hold |
|---|---|---|---|
| take-profit | 159 | **+1.416%** | 2.07h |
| time-stop | 10 | +1.589% | 12.00h |
| trend broke EMA50 | 29 | −0.346% | 1.18h |
| stop-loss | 328 | **−0.517%** | 0.93h |

Winners return 2.7× what losers cost, so the reward:risk geometry is not the
problem — the *mix* is. Converting one stop-loss into a take-profit adds
`(1.416 + 0.517)/526 = 0.0037` points, so **closing the entire gap takes ~12
trades out of 526 — 2.2% of them.**

That framing is more useful and also more alarming: "30% short of costs" sounds
structural, while "12 trades short" makes clear the margin is thin **in both
directions**. `vwap_revert_short` v1/v2 cleared the hurdle by 28% over 279
trades and that is the largest genuine edge this repo has ever recorded.
Nothing here should be read as "we are nearly there".

---
