# Analyst run 2026-07-28 15:05 UTC

## PRE-REGISTRATION (written and committed BEFORE any screen number was known)

This session executes **watchlist item 3** from Jul 27: re-run the book screen
with the new gross columns to separate *coin flips* from *thin edges wearing too
much toll*. One design change from how that item was written, and one addition.

### Change: the screen runs on window B only, not window A

Item 3 said "re-run the screen". Finding 6 of the Jul 27 report established that
**window A (Jun 9–25) is in-sample** for the ~11 strategies authored Jul 5–6 —
their 30-day creation studies span roughly Jun 6 → Jul 6. A gross screen there
would answer "did the tuning stick", not "is there a signal". **Window B
(`--days 14 --end 2026-07-27T00:00`, Jul 13–27) is the only clean window** —
after every parameter freeze in the book. The classification is only meaningful
on clean tape, so window A is not re-run.

### Addition: gross PF alone would misclassify the high-turnover strategies

`gross_pnl` is computed from the filled entry/exit prices, so **slippage
(0.03%/side) is already inside it**; only fees are outside. Gross PF therefore
charges each strategy 0.06% of notional per round trip, in proportion to how
often it trades. Four strategies in this book turn over very fast —
`dispersion_fade` (n=517 on window A, 0.4h holds), `breakout_momentum` (378,
0.5h), `vwap_revert_short_capped` (366, 0.8h), `pump_fade` (144). For those, a
gross PF near 1.0 is **not** proof of "no signal to rescue"; it is equally
consistent with a real signal buried under slippage from over-trading — the
exact category watchlist item 2 wants to surface. Publishing a two-bucket table
off gross PF alone would hand the next session a wrong verdict on precisely the
strategies most likely to respond to cost-aware re-tuning.

So the classification is **three-way**, using a per-trade cost decomposition
derived from `config/settings.yaml` (`fee_taker` 0.0005, `slippage` 0.0003 — both
untouched, per the honesty rules):

- fees round trip = `0.001 × notional` ⇒ **`avg notional ≈ fees_per_trade × 1000`**
- slippage round trip = `0.0006 × notional` ⇒ **`slip/trade ≈ 0.6 × fees_per_trade`**
- **`raw edge/trade = gross_edge_per_trade + 0.6 × fees_per_trade`** (signal before *any* cost)
- **total cost/trade = `1.6 × fees_per_trade`**

| bucket | test | meaning | re-tuning prospect |
|---|---|---|---|
| **A — no raw edge** | raw edge/trade ≤ 0 | directionally wrong before any cost | none; retirement final |
| **B — raw edge, killed by turnover** | raw edge > 0, gross PF ≈ 1.0, short holds / high n | signal exists, slippage eats it | trade less, be more selective |
| **C — raw edge, killed by fees** | gross PF > 1.05, net PF < gross | signal survives slippage, fees finish it | size differently (wider stops → smaller notional) |

Note ahead of the results: `range_fader` (n=108, 2.7h holds) and `sweep_reclaim`
(n=35, 1.7h) are **not** high-turnover, so whatever this screen says, it does not
reverse the Jul 27 vindication of those two retirements. This is a refinement of
that finding, not a contradiction of it.

### Reproducibility: what a deviation will and will not mean

Window B was last run on Jul 27; funding backfill has fired **4 times on Jul 28
alone**. My own memory note says a comparison spanning days is exactly where
backfill legitimately moves a pinned window. Recording the discriminator **now**:

- **Window-B fingerprint at session start (15:10Z):** 74 group symbols,
  **171 distinct symbols** with candles in Jul 13–27, **3,037,271 candle rows**.
  Re-measured at session end.
- Known window-B values to reproduce: **`pdhl_sweep` n=59 / PF 1.07**;
  **`whale_absorb` n=26 / PF 1.32**.
- If those deviate **and** the fingerprint changed, it is **backfill, not
  nondeterminism**, and it is not investigated further. If they deviate and the
  fingerprint is identical, that *is* a methodology problem and takes priority
  over the screen.
- The three-way classification is robust to ±0.05 PF, which is why single-shot
  triage is adequate. **Twins are run only for a number quoted as a decision
  input.** Jul 27 verified single-shot triage reproduced exactly.

### Pre-registered scope

**The expected and most likely correct outcome of this session is zero status
changes.** `whale_absorb` has n=2 live — no verdict is possible or attempted.
Nothing else is eligible. This session's deliverable is the classification table
and the report. If a status changes, it will be because something unforeseen
turned up, and the reason will be stated explicitly.

---
