# Analyst run 2026-07-19 08:20 UTC

Headline: **v3's first forward day is failing (mid_alts SIDE PF 0.52, n=19) — but
it's one BTC-into-BULL day of ~3–5 symbol-episodes, far too thin to act on.**
Hold to the pre-registered gate: no floor revert, no re-tune. The loss is almost
entirely same-symbol repeat-short cascades (TRB 5×, JASMY 6×, ETHFI 2×) fired as
BTC ramped into a strong uptrend. No live edits except a bookkeeping fix
(`capped` DB status → retired, matching the Jul 18 commit).

## Market read
- Regime **BULL** since 21:05Z Jul 18 (conf **1.0**, ADX **36.8**, ema_slope_24h
  **+0.00274**, BTC ~64.7k). A decisive shift out of the SIDE/BEAR chop the book
  is built for. `vwap_revert_short` trades only SIDE/BEAR, so it is **dormant** —
  since the flip it has only *closed* (SYN +306 TP 01:46Z, JASMY +159 time-stop
  07:40Z), no new opens.
- Equity **6624** (−33.75% lifetime), **−725 vs Jul 18** (7349). Today +465 on 2
  closes; the −725 net came from the Jul 18 daytime/evening cascades below.
- 0 open positions.

## Health
- collector + engine alive. One transient `error` (18:44Z Jul 18): MEXC
  `/api/v1/contract/ticker` code 510 — single blip, collector kept running.
  Benign.

## Watchlist #1: v3 forward — FAILING EARLY, but hold (below decision threshold)
v3 (stretch_atr floor 4.5) deployed Jul 18 06:16Z. Forward trades since deploy
(`positions` where `entry_ts > 2026-07-18T06:16Z`, all mid_alts, all SIDE):

**n=19, net −631, PF 0.52, win 15.8%.**

Read the pre-registered rule *precisely* — it has two branches:
- **Promote**: PF ≥ 1.15 **AND** n ≥ 20 **AND** ≥ 5 days → earliest **Jul 23**.
- **Re-examine** (do NOT restore floor 1.5): **n ≥ 20** only.

We are at **n=19 — one SIDE trade short of the re-examine trigger**, not blocked
by the 5-day gate from assessing. And re-examine = *analyze + don't revert*,
which is exactly this report. So today is **hold** on both readings.

### Why this is far weaker than "n=19" suggests (the real reason to hold)
Every one of the 19 entries is dated **Jul 18** (BULL froze the book Jul 19).
**15 of 19 are three symbols' cascades.** By symbol:

| symbol | n | net | wins | note |
|---|---|---|---|---|
| TRB   | 5 | **−439** | 0 | shorted 7.6–8.0 ATR, re-entered ~30s–1min after each stop, all stopped |
| JASMY | 6 | −232 | 1 | 4 rapid re-entries 19:07–19:39, then +159 on the 6th |
| ETHFI | 2 | −170 | 0 | re-entered 49s after the first stop |
| SYN   | 4 | +87 | 1 | 3 stops then +306 TP 47min later |
| GALA  | 2 | +123 | 1 | stop, then +210 TP 29s later |

This is **~3–5 independent symbol-episodes on a single day** as BTC ramped into
BULL (ADX 36.8) — near-zero forward information about the edge. It is *not* 19
independent tests of the ≥4.5 lane. Do not build conclusions on it.

### The loss is a repeat-short cascade, not stretch-lane decay
The bleed is concentrated in re-entering the *same still-ripping symbol* right
after each stop. The engine has **no post-stop re-entry cooldown**:
`account.can_open` blocks only a *concurrent* duplicate — the moment a TRB short
stops, if TRB is still ≥4.5 ATR above VWAP with RSI≥68 it re-shorts on the next
tick (verified: TRB re-entries 29–49s after each exit). SYN/GALA at the *same*
high stretch were profitable, so the stretch floor itself isn't the culprit here.

**But a cooldown is not the fix** — checked against the 19 trades: the winning
reversions frequently arrive *immediately after* a stop (GALA +210 @ 29s after
its stop; JASMY +159 right after 5 straight stops; SYN +306 47min out). A
post-stop cooldown drops those winners with the losers: it cuts net bleed
(≈ −631 → ≈ −287 at a 1h cooldown) but leaves **PF stuck ~0.5** — it buys
smoothness, not edge. Logged as a hypothesis, **not** adopted.

## The live risk during the incubation wait: flickering SIDE labels at trend onset
"Dormant in BULL" is **not** protection. Regime flicker (Jul 17 logged 6
SIDE↔BEAR flips in one hour) can re-enable SIDE for minutes inside what is really
a BULL trend and fire high-stretch shorts into the uptrend. That is the likely
mechanism behind the Jul 18 SIDE-labeled cascades — the labels were *late* as the
ramp began, so the strategy shorted overextension that was actually trend onset,
not reversion. This is the leading candidate for the forward failure (more precise
than a generic cooldown/ADX filter), and the thing to watch: the book can still
bleed on a flicker even while nominally dormant.

## Actions taken this session
- **`vwap_revert_short_capped` DB status → retired** (via `POST /api/strategy/.../status`,
  verified). The Jul 18 commit e44a36b set the *file* meta to retired but never
  flipped the runtime status (it read `paused`) — the exact file-edit≠runtime gap
  the Jul 17 report warned about. Zero trading impact (paused wasn't trading);
  fixes the bookkeeping so the report's claim matches reality. No file/version change.
- **No changes to the live `vwap_revert_short`.** Hold per the pre-registered rule;
  do NOT restore floor 1.5.

## Rejected / not done
- **Revert floor 4.5 → 1.5** — forbidden by the pre-registered rule (sub-4.5
  proven to bleed) and unjustified on one day of data.
- **Add a post-stop cooldown now** — reduces drawdown but not PF (winners follow
  stops); and editing mid-incubation on a 1-day sample is churn. Watchlist, not action.
- **Re-tune / regime-transition filter now** — the hypothesis derives from the
  same single day whose timing does *not* cleanly split winners from losers
  (GALA winner 06:18, SYN winner 14:47, both mid-ramp). Can't resolve mechanism
  on this data. Watchlist.
- **Revive `vwap_revert_short_bull` for BULL coverage** — it's paused and unvalidated;
  reviving without a fresh backtest+held-out pass would be ungated churn. Noted below.

## Watchlist / next session (ranked)
1. **Re-judge v3 at n ≥ 20.** Re-run the forward `positions` split
   (`entry_ts > 2026-07-18T06:16Z`, mid_alts SIDE). If still PF < 1.0 at n ≥ 20,
   the *re-examine* branch fires: **do not restore floor 1.5.** First test the
   **late/flickering-SIDE-label** hypothesis (are the losing entries tagged SIDE
   within minutes of a BULL flip?) before touching the stretch floor. Only
   *promote* if PF ≥ 1.15 at n ≥ 20 **and** ≥ 5 days (≥ Jul 23). NB: BULL keeps
   the book dormant, so n may sit at 19 until SIDE/BEAR returns.
2. **Regime-label latency probe.** Cross the losing cascade entry timestamps
   against the `regime` event log — quantify how many high-stretch shorts fired
   within N minutes of a regime flip. If material, a "no-entry-within-X-of-a-flip"
   or ADX-gate is a cleaner fix than a cooldown or floor change. Data is on hand.
3. **New-edge search (strategic, unchanged, now acute).** The book is ONE dormant
   SIDE/mid_alts short-fade with a failing first forward day; equity −33.75%; and
   we are now *in* a BULL regime with **zero BULL coverage** — the book can only
   lose (on a flicker) until SIDE returns. A second, non-correlated edge (ideally
   with BULL/trend coverage) is the real problem. Queued: OI-confirmed flush
   (`open_interest` collecting), weekend/DoW meme effect; `vwap_revert_short_bull`
   as a *validated* BULL-side candidate (needs backtest+held-out first). Human-in-the-loop.
4. **Follow-up (carried):** funding lookahead for pre-collection periods (Jul 16).
