# Analyst run 2026-07-30 ~06:00 UTC

## PRE-REGISTRATION (written and committed BEFORE any census or backtest number was seen)

Scheduled overnight run. Only ~12.5h of new tape since the Jul 29 session
(17:30Z → 06:02Z), and **zero trades in that span**. That fact alone settles two
of the three ranked watchlist items before any work starts.

### Item 1 — `whale_absorb`: BLOCKED, report only

`/api/report` at 06:02Z: `whale_absorb` **n=8**, unchanged from Jul 29. The gate
wants ≥20 trades and ≥5 days. **No verdict is drawn, and none may be drawn from
this session.** Third consecutive session recording the same thing.

### Item 5 — `open_interest`: feed-shape measurement ONLY, no strategy, no engine change

Measured before writing this: `open_interest` holds **min(ts) 2026-07-05**,
758,742 rows, 214 symbols. **Window A (Jun 9–25) contains zero OI rows.** So no
OI-based strategy can clear this repo's two-window gate today — the only
available split is one 25-day span cut in half, both halves sitting in the same
low-ADX SIDE tape. That is precisely the "one window, same regime" evidence the
last two sessions spent their credibility rejecting.

**Pre-committed:** OI is *not* wired into `ctx` this session and no strategy is
built on it. What is done instead is cheap and useful later — **measure the
feed's shape** (cadence, per-symbol gaps, whether `ts` aligns to candle minutes)
and record it, so the mid-August session (when OI reaches ~5 weeks) is a *test*
session rather than one that discovers a broken feed. Two queries, engine
untouched.

### Item 4 — the 12h time-stop: this session's actual work, census first

`config/settings.yaml` → `max_hold_hours: 12`. Three sessions have called this
"the unexamined global" and nobody has measured it. The cheap version comes
first, on data already in the DB.

**Census:** closed `positions` where `mode='live'`, grouped **by strategy ×
exit_reason** — n, share of that strategy's trades, and net. **Per strategy, not
pooled**: holding periods differ ~3× across the book, so a pooled exit mix is
Simpson-confounded by strategy weighting.

**Stated limitation, up front, so it cannot be over-read later:** the net PnL of
time-stop exits is **not** a causal measure of the time-stop's value. It records
what happened at the 12h mark, not what would have happened without the cap. The
census can only establish whether the lever touches a **material fraction** of
trades. Sign is not evidence of direction.

**Branches, all fixed now:**

1. **T1 — immaterial.** If time-stop exits are <15% of closed live trades
   book-wide AND no single strategy has ≥20 of them → **item 4 closes on
   arithmetic.** The 12h global is not a material lever for this book. No
   backtest is run.
2. **T2 — material.** If any single strategy has **≥20** time-stop exits → run
   the backtest measurement below on that strategy.
3. **T3 — material but positive.** ≥20 time-stop exits whose net is positive →
   still run the measurement (branch T2), because a positive realized net at 12h
   says nothing about the counterfactual. Explicitly **not** grounds to skip.

**The backtest measurement (only if T2/T3 fires):**

- Target = the strategy with the largest time-stop exposure from the census.
- **Window B (Jul 13–27), pinned, `--end 2026-07-27T00:00`, run twice, identical
  n/PF/net required.**
- Exactly **two variants, both directions**: `max_hold_hours` **12 → 6** and
  **12 → 24**, plus the 12 baseline. Three configs, twinned. **No sweep** —
  a search over 4/8/18/36 would be selection on window B.
- **Single-strategy runs** (`--strategy NAME`), specifically to shrink the
  slot-reshuffle confound. That confound is expected to be **larger here than
  for any parameter tested so far**: hold time directly determines how long a
  position occupies one of the 6 slots, so changing it changes which *other*
  entries get taken. Report **n alongside PF** for every config; a large n shift
  means the comparison is partly reshuffle, not effect.
- Patch applied **in-process from a container-local driver**, never into
  `strategies/` or `config/` (both bind-mounted; the live engine hot-reloads).

**Pre-committed regardless of outcome: NO config change and NO status change.**
`max_hold_hours` is engine-wide — it silently re-times every strategy including
the one live experiment. The deliverable is a **measurement handed to the human**,
not an edit. This is fixed now so a favourable number cannot turn into "well,
but it's clearly better".

### Standing checks carried in from prior sessions

- **Re-fingerprint windows A and B before comparing to any Jul 29 number.**
  Window B moved 171→173 symbols inside a pinned window via past-dated backfill
  between Jul 28 and Jul 29. Skipping this reads data drift as an effect.
- **Per-symbol concentration** (Jul 29 Finding 2) on any edge claimed today.
- Any run with a non-empty `.err` is void, not a result.

### ADDENDUM, committed after the census and before the probe below

The census returned **branch T1** (numbers in Finding 1), so **no `max_hold_hours`
backtest is run** — as pre-registered.

The standing re-fingerprint check then produced something worth one cheap probe.
First, a **correction to my own measurement**: my initial fingerprint query
omitted `tf='1m'`, so it counted 1h rows too and appeared to show ~1%/session row
growth in both windows. Re-measured with the filter the drift is ~0.03%
(Finding 3). The probe is registered anyway because the *direction* of the
remaining drift is what matters, not its size.

**Probe:** re-run `pump_fade` on **window B pinned (`--days 14 --end
2026-07-27T00:00`), twice**, and compare against Jul 29's twinned
**n=45 / PF 1.20 / +548.90**.

- **Reproduces exactly** → a pinned window survives a *further* measured data
  change; cross-session comparison is sound at this drift magnitude.
- **Differs** → cross-session numeric comparison is unsound and every stored
  number is valid only within its own session. That would be a significant
  standing-methodology finding.
- **Twins disagree with each other** → nondeterminism; takes priority over
  everything else in this report.

**Pre-committed: this probe cannot change any status.** `pump_fade` was CLOSED on
Jul 29 by gate branch 2 and stays `retired` whatever these two runs print. It is
a reproducibility measurement, not a re-opening — chosen precisely *because* its
expected answer is already known, which is what makes it a test of the method
rather than of the strategy.

### Expected outcome

**Most likely: item 4 closes at T1 on arithmetic, plus an OI feed-shape record
and a no-verdict note on `whale_absorb`.** The book is almost entirely retired,
so there may simply not be enough live time-stop exits to measure. If so, that
is a complete deliverable and the honest one.

---

# OUTCOME (read this first)

- **Item 4 — the 12h time-stop — is CLOSED on arithmetic, at branch T1.** Across
  the book's **entire live history of 532 closed trades**, exactly **10 exited on
  the time-stop: 1.88%.** The most any single strategy has is **6**
  (`vwap_revert_short`), against a gate wanting ≥20. **`max_hold_hours: 12` is
  not a material lever for this book**, and the pre-registered consequence
  applies: no backtest was run and no config was touched. Three sessions ranked
  this as "the unexamined global"; it is now examined, and it was the wrong thing
  to be excited about.
- **The census reconciles to the penny, which makes it a hard result rather than
  an estimate.** The five exit-reason buckets sum to **−3,874.71**, and equity is
  **6,125.29** from a 10,000 start. Every dollar of the drawdown is accounted
  for.
- **Where the money actually goes is not the time-stop — it is stop-losses:
  332 of 532 trades (62.4%), net −33,940.** Take-profits are 160 trades
  (30.1%) at +30,546. The book's whole −3,875 is the residual of two nearly
  equal, enormous flows. **That, not hold time, is the structure worth a future
  session.**
- **`vwap_revert_short` is the only net-positive strategy in the book's entire
  live history: +1,041.11 over 304 trades — and it is `paused`.** This is the
  live-record counterpart to the Jul 21 note that its v1/v2 era was the only
  configuration this repo ever recorded clearing its costs. It sharpens watchlist
  item 8 from a question about a replaced version into a question about the one
  thing that ever worked.
- **The reproducibility probe passed, and strengthens the standing rule.**
  `pump_fade` on pinned window B returned **n=45 / PF 1.20 / +548.90** on both
  twins — identical to Jul 29 *and* Jul 28, across a window that gained ~886 1m
  rows. **Pinned windows are reproducible across sessions at current drift.**
  `pump_fade` remains `retired`; the probe could not and did not change that.
- **Correction to my own first measurement.** My initial fingerprint query
  omitted `tf='1m'` and counted 1h candles, appearing to show ~1%/session row
  growth in both pinned windows. Filtered correctly, window A is **frozen** and
  window B drifted **+0.03%** (Finding 3). Had I reported the first number it
  would have manufactured a methodology crisis out of a bad query.
- **`whale_absorb`: still n=8, still NO verdict** — third consecutive session.
  **Zero trades book-wide in the last 18 hours.**
- **Item 5 (`open_interest`): the feed is healthy and is now characterized** —
  5-minute cadence aligned to candle timestamps, **73 of 74 trading-group symbols
  at 98%+ completeness** (Finding 4). It is also **confirmed unusable until
  mid-August**: window A contains zero OI rows. The mid-August session can now be
  a test session instead of a discovery session.
- **Zero status changes, zero file edits, zero config changes**, as
  pre-registered.

## Health — no action

- Collector and engine alive; server time 06:02Z. Equity **6,125.29 (−38.75%)**,
  **0 open positions, 0 trades today, today's PnL 0.00**.
- **Regime SIDE, confidence 0.42, ADX 9.3**, BTC 64,000. ADX continues to
  collapse: **33.3 (Jul 28) → 14.8 (Jul 29) → 9.3 (today)**. Regime has been
  uninterrupted SIDE since Jul 29 07:03Z — the flickering stopped, but into the
  flattest tape the lab has recorded.
- **No trades in 18 hours.** Last close was `FLOKI_USDT` at Jul 29 12:00Z.
- **MEXC 510s: none since Jul 24 00:54Z** — 6th consecutive quiet session.
- VPS working tree clean apart from the **pre-existing `leadlag_study.py`**,
  still not mine, still untouched (5th session flagged).
- Housekeeping: the api container's `/tmp` still holds **`scr28/` and `var28/`**
  directories from Jul 28. Container-local, not in the git tree, inert. My own
  probe files were deleted at session end (verified).

## The lab-state finding the human should see named

**One strategy is trading.** `whale_absorb` (incubating) is the only non-retired,
non-paused entry in a 16-strategy book, and it produced 8 trades in 4 days. The
live channel is generating roughly **2 trades/day of information** into a 6-slot
engine, in ADX-9 tape.

This is not a bug and nothing here should be reversed — every retirement was
evidence-based, and re-activating a losing strategy to raise the trade count
would be the worst possible reason to trade. But it explains why the last three
watchlists all terminate at *"the next step is a new strategy"*: **the lab has
nearly stopped producing new evidence about itself.** Backtests on pinned
historical windows are now the only real information channel, and those windows
are increasingly in-sample for everything in the book.

---

## Finding 1 — the time-stop census: 1.88%, and item 4 closes

All closed live positions (`mode='live'`, `status='closed'`), by exit reason:

| exit reason | n | share | net |
|---|---|---|---|
| stop-loss | **332** | **62.4%** | **−33,940.20** |
| take-profit | 160 | 30.1% | +30,545.99 |
| trend broke EMA50 (`ema_trend_rider` only) | 29 | 5.5% | −2,124.26 |
| **time-stop** | **10** | **1.88%** | **+1,629.80** |
| bounce window over (`whale_absorb` only) | 1 | 0.2% | +13.96 |
| **total** | **532** | **100%** | **−3,874.71** |

**−3,874.71 against equity 6,125.29 from a 10,000 start: the census accounts for
100% of the lab's drawdown**, so nothing is being missed by a filter.

Time-stop exits, per strategy — the only three that have any:

| strategy | time-stops | that strategy's n | share | net | avg hold |
|---|---|---|---|---|---|
| `vwap_revert_short` | **6** | 304 | 2.0% | +1,323.62 | 12.00h |
| `range_fader` | 3 | 108 | 2.8% | +236.87 | 12.00h |
| `sweep_reclaim` | 1 | 35 | 2.9% | +69.31 | 12.00h |
| *(4 other strategies)* | **0** | 85 | 0% | — | — |

**Branch T1 fires on both of its conditions**: 1.88% is far below the 15%
threshold, and the largest single-strategy count is 6 against a floor of 20. No
`max_hold_hours` backtest was run, per the pre-registration.

Two observations, and one refusal:

- **The cap binds exactly.** All 10 show avg hold 12.00h, confirming the setting
  does what it says.
- **All three buckets are net positive** (+1,629.80 total). It is tempting to
  read that as "trades surviving 12h tend to be winners, so the cap may be
  cutting winners short — raise it".
- **I am not drawing that conclusion, as pre-registered.** The census measures
  what happened *at* the 12h mark, not the counterfactual, and the sample is
  **n=10** — half the judging floor — and survivorship-conditioned by
  construction (a trade only reaches 12h by not having hit its stop). The sign is
  not evidence of direction. What the census establishes is only the thing it
  can: **the lever is too rarely touched to matter**, whichever way it points.

### The finding that displaces item 4

The census was aimed at the time-stop and found a 1.88% sideshow. The number
worth carrying forward is the one it turned up in passing:

| | n | share of trades | net |
|---|---|---|---|
| stop-loss | 332 | **62.4%** | **−33,940** |
| take-profit | 160 | 30.1% | **+30,546** |

The book grinds **±32k of gross flow** to land on **−3,875**. Nearly two thirds
of every trade this lab has ever taken ended at its stop. That is a structural
fact about entry quality and stop placement across seven strategies, and it is
a far larger object than the 12h global — **but note the trap**: this is exactly
the terrain of the stop-distance work that Jul 28 and Jul 29 already closed as a
dead end for `pdhl_sweep`. The lesson from those sessions applies in advance —
**a 62% stop-out rate is a description, not a dial**, and any future session
tempted by it must pre-register a gate and an out-of-sample window before
touching a parameter.

### One incidental note: a discretionary exit was a bigger loss channel than the engine's

`ema_trend_rider`'s own `should_exit` ("trend broke EMA50") accounts for **29 of
its 42 trades (69%) and −2,124.26** — worse than its stop-losses (5 trades,
−559.68) and enough to swamp its take-profits (8, +1,156.40). The strategy is
`retired` so there is no action, but it is a data point for future design:
**a discretionary exit can be the dominant loss channel**, and the engine's
globals are not automatically the thing worth auditing.

## Finding 2 — `whale_absorb`: n=8, unchanged, no verdict

Zero new trades since the Jul 29 session. The full sample remains:

| group | regime at entry | n | net |
|---|---|---|---|
| mid_alts | BEAR | 4 | −115.46 |
| large_alts | BEAR | 1 | −67.41 |
| large_alts | BULL | 1 | −69.95 |
| memes | BEAR | 1 | +13.96 |
| memes | BULL | 1 | −68.70 |
| **total** | | **8** | **−307.56** |

**Every cell is n≤4.** The gate wants ≥20 trades and ≥5 days. **No verdict**, for
the third consecutive session, and `stats_by_group` cannot be read yet — the
`mid_alts` cell that Jul 29 flagged as the one with two-window support holds four
trades.

Worth recording plainly: **the constraint is now tape, not patience.** With ADX
at 9.3 and no trades in 18 hours, the sample is not arriving. If the next session
still finds n<20, the honest options are to accept a much longer incubation or to
state that this strategy cannot be evaluated in this regime — not to lower the
bar.

## Finding 3 — a correction to my own fingerprint, and the pinned windows are stable

**The error first.** My initial fingerprint query counted all timeframes:

| window | unfiltered (wrong) | **`tf='1m'` (correct)** |
|---|---|---|
| A (Jun 9–25) | 3,543,818 | **3,505,802** |
| B (Jul 13–27) | 3,101,710 | **3,077,182** |

The `candles` table holds **12,508,059** 1m rows and **465,545** 1h rows; the
backtester loads `tf='1m'`. The unfiltered numbers implied ~1%/session growth
inside supposedly pinned windows — a methodology crisis that did not exist. Only
the filtered row matters.

**Against Jul 29, filtered:**

| window | Jul 29 | **today** | delta | symbols |
|---|---|---|---|---|
| A | 3,505,982 | **3,505,802** | **−180** | 180 → 180 |
| B | 3,076,469 | **3,077,182** | **+713** | 173 → 173 |

The window-A delta is **−180 with exactly 180 symbols in the window** — one row
per symbol. **Most likely a boundary convention difference**, Jul 29 having used
an inclusive `ts <= end` where I used `ts < end`, which captures exactly one
extra minute-bar per symbol. *(Flagged as inference: I did not recover last
session's SQL.)* On that reading window A is **frozen** and window B gained ~886
rows like-for-like — **+0.03%**, with the symbol set unchanged at 173.

So the Jul 29 concern — past-dated backfill moving pinned windows — is real in
mechanism but **currently ~0.03% in magnitude**, not the 1% my bad query showed.

## Finding 4 — the OI feed is healthy, characterized, and still unusable until mid-August

Measured, no engine touched:

| property | value |
|---|---|
| span | 2026-07-05 13:00Z → 2026-07-30 06:00Z (~25 days) |
| rows / distinct symbols | 758,742 / 214 |
| **cadence** | **5 minutes**, on minutes 0,5,…,55 (~63.3k rows each) |
| **alignment** | **exactly on 1m candle timestamps** → joinable without resampling |
| symbols per day | ~113–125 (the 214 total reflects rotation of non-group names) |
| **trading-group coverage** | **73 of 74 symbols** |
| completeness per group symbol | **avg 7,090 rows, min 7,053** vs 7,200 theoretical (25d × 288) |

Two things make this a *good* record rather than a bare one:

- **`min` is within 0.5% of `avg` (7,053 vs 7,090)** — so coverage is not just
  high on average, **no single group symbol has a hole**. A high mean with a bad
  minimum would have been the trap.
- **The one uncovered group symbol is `BONK_USDT`**, which also has **0 candles**
  — the already-known dead member of `memes`. So the gap is a universe
  housekeeping item, not a feed defect. It also independently corroborates the
  cosmetic issue three sessions have deferred.

**And the blocker is confirmed, not assumed:** window A (Jun 9–25) predates the
feed entirely. Until a second clean window exists after Jul 5, an OI strategy
cannot clear the two-window gate, and a 25-day span split in half is one regime
sampled twice. **Wiring OI into `ctx` was correctly deferred** — that is engine
and backtester work whose payoff is gated on data that does not exist yet.

## Finding 5 — the reproducibility probe passed

`pump_fade`, window B pinned (`--days 14 --end 2026-07-27T00:00`), both `.err`
0 bytes:

| run | n | net PF | net | win | max DD | hold |
|---|---|---|---|---|---|---|
| P1 | 45 | 1.20 | +548.90 | 44.4% | 7.54% | 2.61h |
| **P2** | **45** | **1.20** | **+548.90** | **44.4%** | **7.54%** | **2.61h** |
| *Jul 29 (twinned)* | *45* | *1.20* | *+548.90* | *44.4%* | *7.54%* | *2.61h* |
| *Jul 28 (single)* | *45* | *1.20* | *+548.90* | — | — | — |

**Three sessions, identical to the digit**, across a window that gained ~886 rows
since yesterday and 2 symbols the day before. The by-regime split also reproduces
(SIDE n=38 PF 1.50 / BEAR n=7 PF 0.27).

**Supported claim:** at observed drift magnitudes, **a pinned `--end` gives
results reproducible across sessions, not merely within one.** That is a genuine
strengthening — Jul 29 could only establish within-session twinning plus one
cross-session match.

**Not claimed:** immunity. A newly discovered symbol that pumped inside an old
window would still move an old result; that mechanism is untouched by this
probe. The standing rule stands — **twin every decision input, and re-fingerprint
before citing a number from a previous session.**

`pump_fade` stays **`retired`**. It was closed Jul 29 by gate branch 2 on window
A (PF 0.91) and this probe was pre-committed as unable to change that.

---

## Actions taken

1. **No status changes, in files or DB.** `whale_absorb` stays `incubating`;
   everything else stays as it was.
2. **No strategy file, config file, or universe file edited.** No
   `max_hold_hours` change — pre-committed as out of bounds regardless of
   outcome, because it silently re-times every strategy including the one live
   experiment.
3. **Nothing written to the VPS working tree.** The two probe runs wrote only to
   the api container's `/tmp`, and all four files were deleted at session end
   (verified: `/tmp` now holds only Jul 28's leftover `scr28/` and `var28/`).
4. **Item 4 closed** and item 5 advanced from "wait for data" to a measured feed
   record.

## Rejected / not done

- **Running the `max_hold_hours` 6/24 backtest anyway.** Branch T1 fired and the
  pre-registration said no. With 10 time-stop exits book-wide there is nothing to
  calibrate against, and the runs would have produced numbers whose only use
  would be to argue with the census.
- **Concluding from the +1,629.80 that the time-stop should be raised.** n=10,
  survivorship-conditioned, no counterfactual. Named in the pre-registration
  before the sign was known, specifically so it could not be adopted after.
- **Wiring `open_interest` into `ctx`.** Ruled out before any work: window A has
  zero OI rows, so the gate is unavailable. Deferred to mid-August with the feed
  now characterized.
- **Judging `whale_absorb` at n=8.** Unchanged from Jul 27, 28 and 29.
- **Chasing the 62.4% stop-out rate this session.** It is the biggest object the
  census found, but reaching for it unprepared is how the last two sessions were
  spent. It needs a pre-registered gate and an out-of-sample window, which is a
  session's work, not a coda to this one.
- **Re-activating anything to raise the trade rate.** The idle book is a symptom
  of honest retirements, not a problem to solve by trading.
- **Cosmetic cleanups deferred again** — `BONK_USDT` (now confirmed to have 0
  candles *and* 0 OI), and the `btc_shock_fade` / `vwap_revert_short_bull`
  file-vs-DB status drift. All inert; still a human's call.

## Watchlist / next session (ranked)

1. **`whale_absorb` remains the only live experiment — but the constraint has
   changed from patience to tape.** n=8 after 4 days in ADX-9 SIDE. If the next
   session still finds n<20, decide explicitly between a longer incubation and
   declaring it unevaluable in this regime. **Do not lower the gate.**
2. **The 62.4% stop-loss share is the largest unexplained structure in the live
   record** (332 trades, −33,940, against 160 take-profits at +30,546) and
   displaces the time-stop as the structural question. **Approach it only with a
   pre-registered gate and a held-out window** — it is adjacent to the
   stop-distance work already closed as a dead end, and the same failure mode
   (per-bucket tables at n<20) is waiting.
3. **Item 4 is CLOSED.** `max_hold_hours: 12` touches 1.88% of live trades and no
   strategy has more than 6 such exits. Do not re-rank it as "the unexamined
   global"; it has been examined.
4. **`vwap_revert_short` is the only net-positive strategy in the book's live
   history (+1,041.11 / n=304) and it is `paused`.** Combined with the Jul 21
   finding that its v1/v2 era was the only config ever to clear costs, the
   standing question — why v3 replaced it — is now the single best-supported
   lead in the repo. **Still a human call** per the Jul 21 gate.
5. **`open_interest` in mid-August is the only path to a genuinely new signal.**
   Feed now verified healthy (Finding 4), so that session's work is: wire OI into
   `ctx` (engine + backtester, live/backtest parity), then test. Requires a
   second clean post-Jul-5 window to exist for the two-window gate.
6. **Pinned windows reproduce across sessions** at current drift (Finding 5) —
   but keep twinning decision inputs and **filter `tf='1m'` when fingerprinting**
   (Finding 3), or you will invent drift that isn't there.
7. **Regime has been uninterrupted SIDE since Jul 29 07:03Z with ADX 33→15→9.**
   The flickering that Jul 29 flagged has stopped; the tape is simply flat.
8. **MEXC 510s: none since Jul 24 00:54Z** — 6th consecutive quiet session.
   Monitor only.
